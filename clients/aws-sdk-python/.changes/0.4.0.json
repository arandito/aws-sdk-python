{
  "changes": [
    {
      "type": "dependency",
      "description": "Bump `aws-sdk-bedrock-runtime` from `==0.3.0` to `==0.4.0`."
    },
    {
      "type": "dependency",
      "description": "Bump `aws-sdk-sagemaker-runtime-http2` from `==0.3.0` to `==0.4.0`."
    },
    {
      "type": "dependency",
      "description": "Bump `aws-sdk-transcribe-streaming` from `==0.3.0` to `==0.4.0`."
    }
  ]
}
