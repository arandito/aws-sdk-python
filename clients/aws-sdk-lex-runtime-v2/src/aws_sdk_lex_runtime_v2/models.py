# Code generated by smithy-python-codegen DO NOT EDIT.

from dataclasses import dataclass, field
from enum import StrEnum
import logging
from typing import Any, Literal, Self, Union

from smithy_core.aio.interfaces import StreamingBlob
from smithy_core.deserializers import ShapeDeserializer
from smithy_core.documents import TypeRegistry
from smithy_core.exceptions import ModeledError, SerializationError
from smithy_core.schemas import APIOperation, Schema
from smithy_core.serializers import ShapeSerializer
from smithy_core.shapes import ShapeID
from smithy_core.types import UnknownEnumMixin

from ._private.schemas import (
    ACCESS_DENIED_EXCEPTION as _SCHEMA_ACCESS_DENIED_EXCEPTION,
    ACTIVE_CONTEXT as _SCHEMA_ACTIVE_CONTEXT,
    ACTIVE_CONTEXT_TIME_TO_LIVE as _SCHEMA_ACTIVE_CONTEXT_TIME_TO_LIVE,
    AUDIO_INPUT_EVENT as _SCHEMA_AUDIO_INPUT_EVENT,
    AUDIO_RESPONSE_EVENT as _SCHEMA_AUDIO_RESPONSE_EVENT,
    BAD_GATEWAY_EXCEPTION as _SCHEMA_BAD_GATEWAY_EXCEPTION,
    BUTTON as _SCHEMA_BUTTON,
    CONFIDENCE_SCORE as _SCHEMA_CONFIDENCE_SCORE,
    CONFIGURATION_EVENT as _SCHEMA_CONFIGURATION_EVENT,
    CONFLICT_EXCEPTION as _SCHEMA_CONFLICT_EXCEPTION,
    DELETE_SESSION as _SCHEMA_DELETE_SESSION,
    DELETE_SESSION_INPUT as _SCHEMA_DELETE_SESSION_INPUT,
    DELETE_SESSION_OUTPUT as _SCHEMA_DELETE_SESSION_OUTPUT,
    DEPENDENCY_FAILED_EXCEPTION as _SCHEMA_DEPENDENCY_FAILED_EXCEPTION,
    DIALOG_ACTION as _SCHEMA_DIALOG_ACTION,
    DISCONNECTION_EVENT as _SCHEMA_DISCONNECTION_EVENT,
    DTMF_INPUT_EVENT as _SCHEMA_DTMF_INPUT_EVENT,
    ELICIT_SUB_SLOT as _SCHEMA_ELICIT_SUB_SLOT,
    GET_SESSION as _SCHEMA_GET_SESSION,
    GET_SESSION_INPUT as _SCHEMA_GET_SESSION_INPUT,
    GET_SESSION_OUTPUT as _SCHEMA_GET_SESSION_OUTPUT,
    HEARTBEAT_EVENT as _SCHEMA_HEARTBEAT_EVENT,
    IMAGE_RESPONSE_CARD as _SCHEMA_IMAGE_RESPONSE_CARD,
    INTENT as _SCHEMA_INTENT,
    INTENT_RESULT_EVENT as _SCHEMA_INTENT_RESULT_EVENT,
    INTERNAL_SERVER_EXCEPTION as _SCHEMA_INTERNAL_SERVER_EXCEPTION,
    INTERPRETATION as _SCHEMA_INTERPRETATION,
    MESSAGE as _SCHEMA_MESSAGE,
    PLAYBACK_COMPLETION_EVENT as _SCHEMA_PLAYBACK_COMPLETION_EVENT,
    PLAYBACK_INTERRUPTION_EVENT as _SCHEMA_PLAYBACK_INTERRUPTION_EVENT,
    PUT_SESSION as _SCHEMA_PUT_SESSION,
    PUT_SESSION_INPUT as _SCHEMA_PUT_SESSION_INPUT,
    PUT_SESSION_OUTPUT as _SCHEMA_PUT_SESSION_OUTPUT,
    RECOGNIZED_BOT_MEMBER as _SCHEMA_RECOGNIZED_BOT_MEMBER,
    RECOGNIZE_TEXT as _SCHEMA_RECOGNIZE_TEXT,
    RECOGNIZE_TEXT_INPUT as _SCHEMA_RECOGNIZE_TEXT_INPUT,
    RECOGNIZE_TEXT_OUTPUT as _SCHEMA_RECOGNIZE_TEXT_OUTPUT,
    RECOGNIZE_UTTERANCE as _SCHEMA_RECOGNIZE_UTTERANCE,
    RECOGNIZE_UTTERANCE_INPUT as _SCHEMA_RECOGNIZE_UTTERANCE_INPUT,
    RECOGNIZE_UTTERANCE_OUTPUT as _SCHEMA_RECOGNIZE_UTTERANCE_OUTPUT,
    RESOURCE_NOT_FOUND_EXCEPTION as _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
    RUNTIME_HINTS as _SCHEMA_RUNTIME_HINTS,
    RUNTIME_HINT_DETAILS as _SCHEMA_RUNTIME_HINT_DETAILS,
    RUNTIME_HINT_VALUE as _SCHEMA_RUNTIME_HINT_VALUE,
    SENTIMENT_RESPONSE as _SCHEMA_SENTIMENT_RESPONSE,
    SENTIMENT_SCORE as _SCHEMA_SENTIMENT_SCORE,
    SESSION_STATE as _SCHEMA_SESSION_STATE,
    SLOT as _SCHEMA_SLOT,
    START_CONVERSATION as _SCHEMA_START_CONVERSATION,
    START_CONVERSATION_INPUT as _SCHEMA_START_CONVERSATION_INPUT,
    START_CONVERSATION_OUTPUT as _SCHEMA_START_CONVERSATION_OUTPUT,
    START_CONVERSATION_REQUEST_EVENT_STREAM as _SCHEMA_START_CONVERSATION_REQUEST_EVENT_STREAM,
    START_CONVERSATION_RESPONSE_EVENT_STREAM as _SCHEMA_START_CONVERSATION_RESPONSE_EVENT_STREAM,
    TEXT_INPUT_EVENT as _SCHEMA_TEXT_INPUT_EVENT,
    TEXT_RESPONSE_EVENT as _SCHEMA_TEXT_RESPONSE_EVENT,
    THROTTLING_EXCEPTION as _SCHEMA_THROTTLING_EXCEPTION,
    TRANSCRIPT_EVENT as _SCHEMA_TRANSCRIPT_EVENT,
    VALIDATION_EXCEPTION as _SCHEMA_VALIDATION_EXCEPTION,
    VALUE as _SCHEMA_VALUE,
)


logger = logging.getLogger(__name__)


class ServiceError(ModeledError):
    """
    Base error for all errors in the service.

    Some exceptions do not extend from this class, including
    synthetic, implicit, and shared exception types.
    """


@dataclass(kw_only=True)
class AccessDeniedException(ServiceError):
    """"""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_ACCESS_DENIED_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_ACCESS_DENIED_EXCEPTION.members["message"], self.message
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_ACCESS_DENIED_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_ACCESS_DENIED_EXCEPTION, consumer=_consumer)
        return kwargs


def _serialize_active_context_parameters_map(
    serializer: ShapeSerializer, schema: Schema, value: dict[str, str]
) -> None:
    with serializer.begin_map(schema, len(value)) as m:
        value_schema = schema.members["value"]
        for k, v in value.items():
            m.entry(k, lambda vs: vs.write_string(value_schema, v))


def _deserialize_active_context_parameters_map(
    deserializer: ShapeDeserializer, schema: Schema
) -> dict[str, str]:
    result: dict[str, str] = {}
    value_schema = schema.members["value"]

    def _read_value(k: str, d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result[k] = d.read_string(value_schema)

    deserializer.read_map(schema, _read_value)
    return result


@dataclass(kw_only=True)
class ActiveContextTimeToLive:
    """
    The time that a context is active. You can specify the time to live in
    seconds or in conversation turns.
    """

    time_to_live_in_seconds: int
    """
    The number of seconds that the context is active. You can specify
    between 5 and 86400 seconds (24 hours).
    """

    turns_to_live: int
    """
    The number of turns that the context is active. You can specify up to 20
    turns. Each request and response from the bot is a turn.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_ACTIVE_CONTEXT_TIME_TO_LIVE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_integer(
            _SCHEMA_ACTIVE_CONTEXT_TIME_TO_LIVE.members["timeToLiveInSeconds"],
            self.time_to_live_in_seconds,
        )
        serializer.write_integer(
            _SCHEMA_ACTIVE_CONTEXT_TIME_TO_LIVE.members["turnsToLive"],
            self.turns_to_live,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["time_to_live_in_seconds"] = de.read_integer(
                        _SCHEMA_ACTIVE_CONTEXT_TIME_TO_LIVE.members[
                            "timeToLiveInSeconds"
                        ]
                    )

                case 1:
                    kwargs["turns_to_live"] = de.read_integer(
                        _SCHEMA_ACTIVE_CONTEXT_TIME_TO_LIVE.members["turnsToLive"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_ACTIVE_CONTEXT_TIME_TO_LIVE, consumer=_consumer
        )
        if "time_to_live_in_seconds" not in kwargs:
            kwargs["time_to_live_in_seconds"] = 0
        if "turns_to_live" not in kwargs:
            kwargs["turns_to_live"] = 0
        return kwargs

    @classmethod
    def _smithy_default(cls) -> Self:
        return cls(time_to_live_in_seconds=0, turns_to_live=0)


@dataclass(kw_only=True)
class ActiveContext:
    """
    Contains information about the contexts that a user is using in a
    session. You can configure Amazon Lex V2 to set a context when an intent
    is fulfilled, or you can set a context using the , , or operations.

    Use a context to indicate to Amazon Lex V2 intents that should be used
    as follow-up intents. For example, if the active context is
    `order-fulfilled`, only intents that have `order-fulfilled` configured
    as a trigger are considered for follow up.
    """

    name: str
    """The name of the context."""

    time_to_live: ActiveContextTimeToLive
    """
    Indicates the number of turns or seconds that the context is active.
    Once the time to live expires, the context is no longer returned in a
    response.
    """

    context_attributes: dict[str, str]
    """
    A list of contexts active for the request. A context can be activated
    when a previous intent is fulfilled, or by including the context in the
    request.

    If you don't specify a list of contexts, Amazon Lex V2 will use the
    current list of contexts for the session. If you specify an empty list,
    all contexts for the session are cleared.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_ACTIVE_CONTEXT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(_SCHEMA_ACTIVE_CONTEXT.members["name"], self.name)
        serializer.write_struct(
            _SCHEMA_ACTIVE_CONTEXT.members["timeToLive"], self.time_to_live
        )
        _serialize_active_context_parameters_map(
            serializer,
            _SCHEMA_ACTIVE_CONTEXT.members["contextAttributes"],
            self.context_attributes,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["name"] = de.read_string(
                        _SCHEMA_ACTIVE_CONTEXT.members["name"]
                    )

                case 1:
                    kwargs["time_to_live"] = ActiveContextTimeToLive.deserialize(de)

                case 2:
                    kwargs["context_attributes"] = (
                        _deserialize_active_context_parameters_map(
                            de, _SCHEMA_ACTIVE_CONTEXT.members["contextAttributes"]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_ACTIVE_CONTEXT, consumer=_consumer)
        if "name" not in kwargs:
            kwargs["name"] = ""
        if "time_to_live" not in kwargs:
            kwargs["time_to_live"] = ActiveContextTimeToLive._smithy_default()
        if "context_attributes" not in kwargs:
            kwargs["context_attributes"] = {}
        return kwargs


def _serialize_active_contexts_list(
    serializer: ShapeSerializer, schema: Schema, value: list[ActiveContext]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_active_contexts_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[ActiveContext]:
    result: list[ActiveContext] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(ActiveContext.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class AudioInputEvent:
    """
    Represents a chunk of audio sent from the client application to Amazon
    Lex V2. The audio is all or part of an utterance from the user.

    Amazon Lex V2 accumulates audio chunks until it recognizes a natural
    pause in speech before processing the input.
    """

    content_type: str
    """
    The encoding used for the audio chunk. You must use 8 KHz PCM 16-bit
    mono-channel little-endian format. The value of the field should be:

    `audio/lpcm; sample-rate=8000; sample-size-bits=16; channel-count=1; is-big-endian=false`
    """

    audio_chunk: bytes | None = None
    """An encoded stream of audio."""

    event_id: str | None = None
    """
    A unique identifier that your application assigns to the event. You can
    use this to identify events in logs.
    """

    client_timestamp_millis: int = 0
    """
    A timestamp set by the client of the date and time that the event was
    sent to Amazon Lex V2.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_AUDIO_INPUT_EVENT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.audio_chunk is not None:
            serializer.write_blob(
                _SCHEMA_AUDIO_INPUT_EVENT.members["audioChunk"], self.audio_chunk
            )

        serializer.write_string(
            _SCHEMA_AUDIO_INPUT_EVENT.members["contentType"], self.content_type
        )
        if self.event_id is not None:
            serializer.write_string(
                _SCHEMA_AUDIO_INPUT_EVENT.members["eventId"], self.event_id
            )

        serializer.write_long(
            _SCHEMA_AUDIO_INPUT_EVENT.members["clientTimestampMillis"],
            self.client_timestamp_millis,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["audio_chunk"] = de.read_blob(
                        _SCHEMA_AUDIO_INPUT_EVENT.members["audioChunk"]
                    )

                case 1:
                    kwargs["content_type"] = de.read_string(
                        _SCHEMA_AUDIO_INPUT_EVENT.members["contentType"]
                    )

                case 2:
                    kwargs["event_id"] = de.read_string(
                        _SCHEMA_AUDIO_INPUT_EVENT.members["eventId"]
                    )

                case 3:
                    kwargs["client_timestamp_millis"] = de.read_long(
                        _SCHEMA_AUDIO_INPUT_EVENT.members["clientTimestampMillis"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_AUDIO_INPUT_EVENT, consumer=_consumer)
        if "content_type" not in kwargs:
            kwargs["content_type"] = ""
        return kwargs


@dataclass(kw_only=True)
class AudioResponseEvent:
    """
    An event sent from Amazon Lex V2 to your client application containing
    audio to play to the user.
    """

    audio_chunk: bytes | None = None
    """A chunk of the audio to play."""

    content_type: str | None = None
    """
    The encoding of the audio chunk. This is the same as the encoding
    configure in the `contentType` field of the `ConfigurationEvent`.
    """

    event_id: str | None = None
    """
    A unique identifier of the event sent by Amazon Lex V2. The identifier
    is in the form `RESPONSE-N`, where N is a number starting with one and
    incremented for each event sent by Amazon Lex V2 in the current session.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_AUDIO_RESPONSE_EVENT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.audio_chunk is not None:
            serializer.write_blob(
                _SCHEMA_AUDIO_RESPONSE_EVENT.members["audioChunk"], self.audio_chunk
            )

        if self.content_type is not None:
            serializer.write_string(
                _SCHEMA_AUDIO_RESPONSE_EVENT.members["contentType"], self.content_type
            )

        if self.event_id is not None:
            serializer.write_string(
                _SCHEMA_AUDIO_RESPONSE_EVENT.members["eventId"], self.event_id
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["audio_chunk"] = de.read_blob(
                        _SCHEMA_AUDIO_RESPONSE_EVENT.members["audioChunk"]
                    )

                case 1:
                    kwargs["content_type"] = de.read_string(
                        _SCHEMA_AUDIO_RESPONSE_EVENT.members["contentType"]
                    )

                case 2:
                    kwargs["event_id"] = de.read_string(
                        _SCHEMA_AUDIO_RESPONSE_EVENT.members["eventId"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_AUDIO_RESPONSE_EVENT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class ConflictException(ServiceError):
    """"""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_CONFLICT_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_CONFLICT_EXCEPTION.members["message"], self.message
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_CONFLICT_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_CONFLICT_EXCEPTION, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class DeleteSessionInput:
    """Dataclass for DeleteSessionInput structure."""

    bot_id: str | None = None
    """The identifier of the bot that contains the session data."""

    bot_alias_id: str | None = None
    """The alias identifier in use for the bot that contains the session data."""

    locale_id: str | None = None
    """The locale where the session is in use."""

    session_id: str | None = None
    """The identifier of the session to delete."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DELETE_SESSION_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.bot_id is not None:
            serializer.write_string(
                _SCHEMA_DELETE_SESSION_INPUT.members["botId"], self.bot_id
            )

        if self.bot_alias_id is not None:
            serializer.write_string(
                _SCHEMA_DELETE_SESSION_INPUT.members["botAliasId"], self.bot_alias_id
            )

        if self.locale_id is not None:
            serializer.write_string(
                _SCHEMA_DELETE_SESSION_INPUT.members["localeId"], self.locale_id
            )

        if self.session_id is not None:
            serializer.write_string(
                _SCHEMA_DELETE_SESSION_INPUT.members["sessionId"], self.session_id
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["bot_id"] = de.read_string(
                        _SCHEMA_DELETE_SESSION_INPUT.members["botId"]
                    )

                case 1:
                    kwargs["bot_alias_id"] = de.read_string(
                        _SCHEMA_DELETE_SESSION_INPUT.members["botAliasId"]
                    )

                case 2:
                    kwargs["locale_id"] = de.read_string(
                        _SCHEMA_DELETE_SESSION_INPUT.members["localeId"]
                    )

                case 3:
                    kwargs["session_id"] = de.read_string(
                        _SCHEMA_DELETE_SESSION_INPUT.members["sessionId"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_DELETE_SESSION_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class DeleteSessionOutput:
    """Dataclass for DeleteSessionOutput structure."""

    bot_id: str | None = None
    """The identifier of the bot that contained the session data."""

    bot_alias_id: str | None = None
    """The alias identifier in use for the bot that contained the session data."""

    locale_id: str | None = None
    """The locale where the session was used."""

    session_id: str | None = None
    """The identifier of the deleted session."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DELETE_SESSION_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.bot_id is not None:
            serializer.write_string(
                _SCHEMA_DELETE_SESSION_OUTPUT.members["botId"], self.bot_id
            )

        if self.bot_alias_id is not None:
            serializer.write_string(
                _SCHEMA_DELETE_SESSION_OUTPUT.members["botAliasId"], self.bot_alias_id
            )

        if self.locale_id is not None:
            serializer.write_string(
                _SCHEMA_DELETE_SESSION_OUTPUT.members["localeId"], self.locale_id
            )

        if self.session_id is not None:
            serializer.write_string(
                _SCHEMA_DELETE_SESSION_OUTPUT.members["sessionId"], self.session_id
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["bot_id"] = de.read_string(
                        _SCHEMA_DELETE_SESSION_OUTPUT.members["botId"]
                    )

                case 1:
                    kwargs["bot_alias_id"] = de.read_string(
                        _SCHEMA_DELETE_SESSION_OUTPUT.members["botAliasId"]
                    )

                case 2:
                    kwargs["locale_id"] = de.read_string(
                        _SCHEMA_DELETE_SESSION_OUTPUT.members["localeId"]
                    )

                case 3:
                    kwargs["session_id"] = de.read_string(
                        _SCHEMA_DELETE_SESSION_OUTPUT.members["sessionId"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_DELETE_SESSION_OUTPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class InternalServerException(ServiceError):
    """"""

    fault: Literal["client", "server"] | None = "server"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_INTERNAL_SERVER_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_INTERNAL_SERVER_EXCEPTION.members["message"], self.message
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_INTERNAL_SERVER_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_INTERNAL_SERVER_EXCEPTION, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class ResourceNotFoundException(ServiceError):
    """"""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION.members["message"], self.message
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class ThrottlingException(ServiceError):
    """"""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_THROTTLING_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_THROTTLING_EXCEPTION.members["message"], self.message
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_THROTTLING_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_THROTTLING_EXCEPTION, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class ValidationException(ServiceError):
    """"""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_VALIDATION_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_VALIDATION_EXCEPTION.members["message"], self.message
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_VALIDATION_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_VALIDATION_EXCEPTION, consumer=_consumer)
        return kwargs


DELETE_SESSION = APIOperation(
    input=DeleteSessionInput,
    output=DeleteSessionOutput,
    schema=_SCHEMA_DELETE_SESSION,
    input_schema=_SCHEMA_DELETE_SESSION_INPUT,
    output_schema=_SCHEMA_DELETE_SESSION_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID(
                "com.amazonaws.lexruntimev2#AccessDeniedException"
            ): AccessDeniedException,
            ShapeID("com.amazonaws.lexruntimev2#ConflictException"): ConflictException,
            ShapeID(
                "com.amazonaws.lexruntimev2#InternalServerException"
            ): InternalServerException,
            ShapeID(
                "com.amazonaws.lexruntimev2#ResourceNotFoundException"
            ): ResourceNotFoundException,
            ShapeID(
                "com.amazonaws.lexruntimev2#ThrottlingException"
            ): ThrottlingException,
            ShapeID(
                "com.amazonaws.lexruntimev2#ValidationException"
            ): ValidationException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_ACCESS_DENIED_EXCEPTION,
        _SCHEMA_CONFLICT_EXCEPTION,
        _SCHEMA_INTERNAL_SERVER_EXCEPTION,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
        _SCHEMA_THROTTLING_EXCEPTION,
        _SCHEMA_VALIDATION_EXCEPTION,
    ],
)


@dataclass(kw_only=True)
class GetSessionInput:
    """Dataclass for GetSessionInput structure."""

    bot_id: str | None = None
    """The identifier of the bot that contains the session data."""

    bot_alias_id: str | None = None
    """The alias identifier in use for the bot that contains the session data."""

    locale_id: str | None = None
    """The locale where the session is in use."""

    session_id: str | None = None
    """The identifier of the session to return."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_GET_SESSION_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.bot_id is not None:
            serializer.write_string(
                _SCHEMA_GET_SESSION_INPUT.members["botId"], self.bot_id
            )

        if self.bot_alias_id is not None:
            serializer.write_string(
                _SCHEMA_GET_SESSION_INPUT.members["botAliasId"], self.bot_alias_id
            )

        if self.locale_id is not None:
            serializer.write_string(
                _SCHEMA_GET_SESSION_INPUT.members["localeId"], self.locale_id
            )

        if self.session_id is not None:
            serializer.write_string(
                _SCHEMA_GET_SESSION_INPUT.members["sessionId"], self.session_id
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["bot_id"] = de.read_string(
                        _SCHEMA_GET_SESSION_INPUT.members["botId"]
                    )

                case 1:
                    kwargs["bot_alias_id"] = de.read_string(
                        _SCHEMA_GET_SESSION_INPUT.members["botAliasId"]
                    )

                case 2:
                    kwargs["locale_id"] = de.read_string(
                        _SCHEMA_GET_SESSION_INPUT.members["localeId"]
                    )

                case 3:
                    kwargs["session_id"] = de.read_string(
                        _SCHEMA_GET_SESSION_INPUT.members["sessionId"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_GET_SESSION_INPUT, consumer=_consumer)
        return kwargs


class ConfirmationState(UnknownEnumMixin, StrEnum):
    CONFIRMED = "Confirmed"
    DENIED = "Denied"
    NONE = "None"


class Shape(UnknownEnumMixin, StrEnum):
    SCALAR = "Scalar"
    LIST_ = "List"
    COMPOSITE = "Composite"


def _serialize_string_list(
    serializer: ShapeSerializer, schema: Schema, value: list[str]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_string(member_schema, e)


def _deserialize_string_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[str]:
    result: list[str] = []
    member_schema = schema.members["member"]

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(d.read_string(member_schema))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class Value:
    """
    Information about the value provided for a slot and Amazon Lex V2's
    interpretation.
    """

    interpreted_value: str
    """
    The value that Amazon Lex V2 determines for the slot, given the user
    input. The actual value depends on the setting of the value selection
    strategy for the bot. You can choose to use the value entered by the
    user, or you can have Amazon Lex V2 choose the first value in the
    `resolvedValues` list.
    """

    original_value: str | None = None
    """
    The part of the user's response to the slot elicitation that Amazon Lex
    V2 determines is relevant to the slot value.
    """

    resolved_values: list[str] | None = None
    """
    A list of values that Amazon Lex V2 determines are possible resolutions
    for the user input. The first value matches the `interpretedValue`.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_VALUE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.original_value is not None:
            serializer.write_string(
                _SCHEMA_VALUE.members["originalValue"], self.original_value
            )

        serializer.write_string(
            _SCHEMA_VALUE.members["interpretedValue"], self.interpreted_value
        )
        if self.resolved_values is not None:
            _serialize_string_list(
                serializer,
                _SCHEMA_VALUE.members["resolvedValues"],
                self.resolved_values,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["original_value"] = de.read_string(
                        _SCHEMA_VALUE.members["originalValue"]
                    )

                case 1:
                    kwargs["interpreted_value"] = de.read_string(
                        _SCHEMA_VALUE.members["interpretedValue"]
                    )

                case 2:
                    kwargs["resolved_values"] = _deserialize_string_list(
                        de, _SCHEMA_VALUE.members["resolvedValues"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_VALUE, consumer=_consumer)
        if "interpreted_value" not in kwargs:
            kwargs["interpreted_value"] = ""
        return kwargs


class IntentState(UnknownEnumMixin, StrEnum):
    FAILED = "Failed"
    FULFILLED = "Fulfilled"
    IN_PROGRESS = "InProgress"
    READY_FOR_FULFILLMENT = "ReadyForFulfillment"
    WAITING = "Waiting"
    FULFILLMENT_IN_PROGRESS = "FulfillmentInProgress"


class InterpretationSource(UnknownEnumMixin, StrEnum):
    BEDROCK = "Bedrock"
    LEX = "Lex"


@dataclass(kw_only=True)
class ConfidenceScore:
    """
    Provides a score that indicates the confidence that Amazon Lex V2 has
    that an intent is the one that satisfies the user's intent.
    """

    score: float = 0
    """
    A score that indicates how confident Amazon Lex V2 is that an intent
    satisfies the user's intent. Ranges between 0.00 and 1.00. Higher
    scores indicate higher confidence.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_CONFIDENCE_SCORE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_double(_SCHEMA_CONFIDENCE_SCORE.members["score"], self.score)

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["score"] = de.read_double(
                        _SCHEMA_CONFIDENCE_SCORE.members["score"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_CONFIDENCE_SCORE, consumer=_consumer)
        return kwargs


class SentimentType(UnknownEnumMixin, StrEnum):
    MIXED = "MIXED"
    NEGATIVE = "NEGATIVE"
    NEUTRAL = "NEUTRAL"
    POSITIVE = "POSITIVE"


@dataclass(kw_only=True)
class SentimentScore:
    """The individual sentiment responses for the utterance."""

    positive: float = 0
    """
    The level of confidence that Amazon Comprehend has in the accuracy of
    its detection of the `POSITIVE` sentiment.
    """

    negative: float = 0
    """
    The level of confidence that Amazon Comprehend has in the accuracy of
    its detection of the `NEGATIVE` sentiment.
    """

    neutral: float = 0
    """
    The level of confidence that Amazon Comprehend has in the accuracy of
    its detection of the `NEUTRAL` sentiment.
    """

    mixed: float = 0
    """
    The level of confidence that Amazon Comprehend has in the accuracy of
    its detection of the `MIXED` sentiment.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_SENTIMENT_SCORE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_double(
            _SCHEMA_SENTIMENT_SCORE.members["positive"], self.positive
        )
        serializer.write_double(
            _SCHEMA_SENTIMENT_SCORE.members["negative"], self.negative
        )
        serializer.write_double(
            _SCHEMA_SENTIMENT_SCORE.members["neutral"], self.neutral
        )
        serializer.write_double(_SCHEMA_SENTIMENT_SCORE.members["mixed"], self.mixed)

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["positive"] = de.read_double(
                        _SCHEMA_SENTIMENT_SCORE.members["positive"]
                    )

                case 1:
                    kwargs["negative"] = de.read_double(
                        _SCHEMA_SENTIMENT_SCORE.members["negative"]
                    )

                case 2:
                    kwargs["neutral"] = de.read_double(
                        _SCHEMA_SENTIMENT_SCORE.members["neutral"]
                    )

                case 3:
                    kwargs["mixed"] = de.read_double(
                        _SCHEMA_SENTIMENT_SCORE.members["mixed"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_SENTIMENT_SCORE, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class SentimentResponse:
    """
    Provides information about the sentiment expressed in a user's response
    in a conversation. Sentiments are determined using Amazon Comprehend.
    Sentiments are only returned if they are enabled for the bot.

    For more information, see [Determine
    Sentiment](https://docs.aws.amazon.com/comprehend/latest/dg/how-sentiment.html)
    in the *Amazon Comprehend developer guide*.
    """

    sentiment: str | None = None
    """
    The overall sentiment expressed in the user's response. This is the
    sentiment most likely expressed by the user based on the analysis by
    Amazon Comprehend.
    """

    sentiment_score: SentimentScore | None = None
    """The individual sentiment responses for the utterance."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_SENTIMENT_RESPONSE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.sentiment is not None:
            serializer.write_string(
                _SCHEMA_SENTIMENT_RESPONSE.members["sentiment"], self.sentiment
            )

        if self.sentiment_score is not None:
            serializer.write_struct(
                _SCHEMA_SENTIMENT_RESPONSE.members["sentimentScore"],
                self.sentiment_score,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["sentiment"] = SentimentType(
                        de.read_string(_SCHEMA_SENTIMENT_RESPONSE.members["sentiment"])
                    )

                case 1:
                    kwargs["sentiment_score"] = SentimentScore.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_SENTIMENT_RESPONSE, consumer=_consumer)
        return kwargs


class MessageContentType(UnknownEnumMixin, StrEnum):
    CUSTOM_PAYLOAD = "CustomPayload"
    IMAGE_RESPONSE_CARD = "ImageResponseCard"
    PLAIN_TEXT = "PlainText"
    SSML = "SSML"


@dataclass(kw_only=True)
class Button:
    """A button that appears on a response card show to the user."""

    text: str
    """The text that is displayed on the button."""

    value: str
    """The value returned to Amazon Lex V2 when a user chooses the button."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_BUTTON, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(_SCHEMA_BUTTON.members["text"], self.text)
        serializer.write_string(_SCHEMA_BUTTON.members["value"], self.value)

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["text"] = de.read_string(_SCHEMA_BUTTON.members["text"])

                case 1:
                    kwargs["value"] = de.read_string(_SCHEMA_BUTTON.members["value"])

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_BUTTON, consumer=_consumer)
        if "text" not in kwargs:
            kwargs["text"] = ""
        if "value" not in kwargs:
            kwargs["value"] = ""
        return kwargs


def _serialize_buttons_list(
    serializer: ShapeSerializer, schema: Schema, value: list[Button]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_buttons_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[Button]:
    result: list[Button] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(Button.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class ImageResponseCard:
    """
    A card that is shown to the user by a messaging platform. You define the
    contents of the card, the card is displayed by the platform.

    When you use a response card, the response from the user is constrained
    to the text associated with a button on the card.
    """

    title: str
    """
    The title to display on the response card. The format of the title is
    determined by the platform displaying the response card.
    """

    subtitle: str | None = None
    """
    The subtitle to display on the response card. The format of the subtitle
    is determined by the platform displaying the response card.
    """

    image_url: str | None = None
    """
    The URL of an image to display on the response card. The image URL must
    be publicly available so that the platform displaying the response card
    has access to the image.
    """

    buttons: list[Button] | None = None
    """
    A list of buttons that should be displayed on the response card. The
    arrangement of the buttons is determined by the platform that displays
    the button.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_IMAGE_RESPONSE_CARD, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_IMAGE_RESPONSE_CARD.members["title"], self.title
        )
        if self.subtitle is not None:
            serializer.write_string(
                _SCHEMA_IMAGE_RESPONSE_CARD.members["subtitle"], self.subtitle
            )

        if self.image_url is not None:
            serializer.write_string(
                _SCHEMA_IMAGE_RESPONSE_CARD.members["imageUrl"], self.image_url
            )

        if self.buttons is not None:
            _serialize_buttons_list(
                serializer, _SCHEMA_IMAGE_RESPONSE_CARD.members["buttons"], self.buttons
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["title"] = de.read_string(
                        _SCHEMA_IMAGE_RESPONSE_CARD.members["title"]
                    )

                case 1:
                    kwargs["subtitle"] = de.read_string(
                        _SCHEMA_IMAGE_RESPONSE_CARD.members["subtitle"]
                    )

                case 2:
                    kwargs["image_url"] = de.read_string(
                        _SCHEMA_IMAGE_RESPONSE_CARD.members["imageUrl"]
                    )

                case 3:
                    kwargs["buttons"] = _deserialize_buttons_list(
                        de, _SCHEMA_IMAGE_RESPONSE_CARD.members["buttons"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_IMAGE_RESPONSE_CARD, consumer=_consumer)
        if "title" not in kwargs:
            kwargs["title"] = ""
        return kwargs


@dataclass(kw_only=True)
class Message:
    """Container for text that is returned to the customer.."""

    content_type: str
    """Indicates the type of response."""

    content: str | None = field(repr=False, default=None)
    """The text of the message."""

    image_response_card: ImageResponseCard | None = None
    """
    A card that is shown to the user by a messaging platform. You define the
    contents of the card, the card is displayed by the platform.

    When you use a response card, the response from the user is constrained
    to the text associated with a button on the card.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_MESSAGE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.content is not None:
            serializer.write_string(_SCHEMA_MESSAGE.members["content"], self.content)

        serializer.write_string(
            _SCHEMA_MESSAGE.members["contentType"], self.content_type
        )
        if self.image_response_card is not None:
            serializer.write_struct(
                _SCHEMA_MESSAGE.members["imageResponseCard"], self.image_response_card
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["content"] = de.read_string(
                        _SCHEMA_MESSAGE.members["content"]
                    )

                case 1:
                    kwargs["content_type"] = MessageContentType(
                        de.read_string(_SCHEMA_MESSAGE.members["contentType"])
                    )

                case 2:
                    kwargs["image_response_card"] = ImageResponseCard.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_MESSAGE, consumer=_consumer)
        if "content_type" not in kwargs:
            kwargs["content_type"] = MessageContentType._corrected("")
        return kwargs


def _serialize_messages(
    serializer: ShapeSerializer, schema: Schema, value: list[Message]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_messages(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[Message]:
    result: list[Message] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(Message.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


class StyleType(UnknownEnumMixin, StrEnum):
    DEFAULT = "Default"
    SPELL_BY_LETTER = "SpellByLetter"
    SPELL_BY_WORD = "SpellByWord"


class DialogActionType(UnknownEnumMixin, StrEnum):
    CLOSE = "Close"
    CONFIRM_INTENT = "ConfirmIntent"
    DELEGATE = "Delegate"
    ELICIT_INTENT = "ElicitIntent"
    ELICIT_SLOT = "ElicitSlot"
    NONE = "None"


@dataclass(kw_only=True)
class RuntimeHintValue:
    """
    Provides the phrase that Amazon Lex V2 should look for in the user's
    input to the bot.
    """

    phrase: str
    """
    The phrase that Amazon Lex V2 should look for in the user's input to
    the bot.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_RUNTIME_HINT_VALUE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_RUNTIME_HINT_VALUE.members["phrase"], self.phrase
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["phrase"] = de.read_string(
                        _SCHEMA_RUNTIME_HINT_VALUE.members["phrase"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_RUNTIME_HINT_VALUE, consumer=_consumer)
        if "phrase" not in kwargs:
            kwargs["phrase"] = ""
        return kwargs


def _serialize_runtime_hint_values_list(
    serializer: ShapeSerializer, schema: Schema, value: list[RuntimeHintValue]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_runtime_hint_values_list(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[RuntimeHintValue]:
    result: list[RuntimeHintValue] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(RuntimeHintValue.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


def _serialize_string_map(
    serializer: ShapeSerializer, schema: Schema, value: dict[str, str]
) -> None:
    with serializer.begin_map(schema, len(value)) as m:
        value_schema = schema.members["value"]
        for k, v in value.items():
            m.entry(k, lambda vs: vs.write_string(value_schema, v))


def _deserialize_string_map(
    deserializer: ShapeDeserializer, schema: Schema
) -> dict[str, str]:
    result: dict[str, str] = {}
    value_schema = schema.members["value"]

    def _read_value(k: str, d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result[k] = d.read_string(value_schema)

    deserializer.read_map(schema, _read_value)
    return result


@dataclass(kw_only=True)
class BadGatewayException(ServiceError):
    """"""

    fault: Literal["client", "server"] | None = "server"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_BAD_GATEWAY_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_BAD_GATEWAY_EXCEPTION.members["message"], self.message
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_BAD_GATEWAY_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_BAD_GATEWAY_EXCEPTION, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class DependencyFailedException(ServiceError):
    """"""

    fault: Literal["client", "server"] | None = "client"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DEPENDENCY_FAILED_EXCEPTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_DEPENDENCY_FAILED_EXCEPTION.members["message"], self.message
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["message"] = de.read_string(
                        _SCHEMA_DEPENDENCY_FAILED_EXCEPTION.members["message"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_DEPENDENCY_FAILED_EXCEPTION, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class PutSessionOutput:
    """Dataclass for PutSessionOutput structure."""

    content_type: str | None = None
    """
    The type of response. Same as the type specified in the
    `responseContentType` field in the request.
    """

    messages: str | None = None
    """
    A list of messages that were last sent to the user. The messages are
    ordered based on how you return the messages from you Lambda function or
    the order that the messages are defined in the bot.
    """

    session_state: str | None = None
    """
    A base-64-encoded gzipped field that represents the current state of the
    dialog between the user and the bot. Use this to determine the progress
    of the conversation and what the next action may be.
    """

    request_attributes: str | None = None
    """
    A base-64-encoded gzipped field that provides request-specific
    information passed between the client application and Amazon Lex V2.
    These are the same as the `requestAttribute` parameter in the call to
    the `PutSession` operation.
    """

    session_id: str | None = None
    """The identifier of the session that received the data."""

    audio_stream: StreamingBlob = b""
    """
    If the requested content type was audio, the audio version of the
    message to convey to the user.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_PUT_SESSION_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.content_type is not None:
            serializer.write_string(
                _SCHEMA_PUT_SESSION_OUTPUT.members["contentType"], self.content_type
            )

        if self.messages is not None:
            serializer.write_string(
                _SCHEMA_PUT_SESSION_OUTPUT.members["messages"], self.messages
            )

        if self.session_state is not None:
            serializer.write_string(
                _SCHEMA_PUT_SESSION_OUTPUT.members["sessionState"], self.session_state
            )

        if self.request_attributes is not None:
            serializer.write_string(
                _SCHEMA_PUT_SESSION_OUTPUT.members["requestAttributes"],
                self.request_attributes,
            )

        if self.session_id is not None:
            serializer.write_string(
                _SCHEMA_PUT_SESSION_OUTPUT.members["sessionId"], self.session_id
            )

        serializer.write_data_stream(
            _SCHEMA_PUT_SESSION_OUTPUT.members["audioStream"], self.audio_stream
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["content_type"] = de.read_string(
                        _SCHEMA_PUT_SESSION_OUTPUT.members["contentType"]
                    )

                case 1:
                    kwargs["messages"] = de.read_string(
                        _SCHEMA_PUT_SESSION_OUTPUT.members["messages"]
                    )

                case 2:
                    kwargs["session_state"] = de.read_string(
                        _SCHEMA_PUT_SESSION_OUTPUT.members["sessionState"]
                    )

                case 3:
                    kwargs["request_attributes"] = de.read_string(
                        _SCHEMA_PUT_SESSION_OUTPUT.members["requestAttributes"]
                    )

                case 4:
                    kwargs["session_id"] = de.read_string(
                        _SCHEMA_PUT_SESSION_OUTPUT.members["sessionId"]
                    )

                case 5:
                    kwargs["audio_stream"] = de.read_data_stream(
                        _SCHEMA_PUT_SESSION_OUTPUT.members["audioStream"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_PUT_SESSION_OUTPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class RecognizedBotMember:
    """The bot member that processes the request."""

    bot_id: str
    """The identifier of the bot member that processes the request."""

    bot_name: str | None = None
    """The name of the bot member that processes the request."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_RECOGNIZED_BOT_MEMBER, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_RECOGNIZED_BOT_MEMBER.members["botId"], self.bot_id
        )
        if self.bot_name is not None:
            serializer.write_string(
                _SCHEMA_RECOGNIZED_BOT_MEMBER.members["botName"], self.bot_name
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["bot_id"] = de.read_string(
                        _SCHEMA_RECOGNIZED_BOT_MEMBER.members["botId"]
                    )

                case 1:
                    kwargs["bot_name"] = de.read_string(
                        _SCHEMA_RECOGNIZED_BOT_MEMBER.members["botName"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_RECOGNIZED_BOT_MEMBER, consumer=_consumer)
        if "bot_id" not in kwargs:
            kwargs["bot_id"] = ""
        return kwargs


@dataclass(kw_only=True)
class RecognizeUtteranceInput:
    """Dataclass for RecognizeUtteranceInput structure."""

    bot_id: str | None = None
    """The identifier of the bot that should receive the request."""

    bot_alias_id: str | None = None
    """The alias identifier in use for the bot that should receive the request."""

    locale_id: str | None = None
    """The locale where the session is in use."""

    session_id: str | None = None
    """The identifier of the session in use."""

    session_state: str | None = field(repr=False, default=None)
    """
    Sets the state of the session with the user. You can use this to set the
    current intent, attributes, context, and dialog action. Use the dialog
    action to determine the next step that Amazon Lex V2 should use in the
    conversation with the user.

    The `sessionState` field must be compressed using gzip and then base64
    encoded before sending to Amazon Lex V2.
    """

    request_attributes: str | None = field(repr=False, default=None)
    """
    Request-specific information passed between the client application and
    Amazon Lex V2

    The namespace `x-amz-lex:` is reserved for special attributes. Don't
    create any request attributes for prefix `x-amz-lex:`.

    The `requestAttributes` field must be compressed using gzip and then
    base64 encoded before sending to Amazon Lex V2.
    """

    request_content_type: str | None = None
    """
    Indicates the format for audio input or that the content is text. The
    header must start with one of the following prefixes:

    - PCM format, audio data must be in little-endian byte order.

      - audio/l16; rate=16000; channels=1

      - audio/x-l16; sample-rate=16000; channel-count=1

      - audio/lpcm; sample-rate=8000; sample-size-bits=16; channel-count=1;
        is-big-endian=false

    - Opus format

      - audio/x-cbr-opus-with-preamble;preamble-size=0;bit-rate=256000;frame-size-milliseconds=4

    - Text format

      - text/plain; charset=utf-8
    """

    response_content_type: str | None = None
    """
    The message that Amazon Lex V2 returns in the response can be either
    text or speech based on the `responseContentType` value.

    - If the value is `text/plain;charset=utf-8`, Amazon Lex V2 returns text
      in the response.

    - If the value begins with `audio/`, Amazon Lex V2 returns speech in the
      response. Amazon Lex V2 uses Amazon Polly to generate the speech using
      the configuration that you specified in the `responseContentType`
      parameter. For example, if you specify `audio/mpeg` as the value,
      Amazon Lex V2 returns speech in the MPEG format.

    - If the value is `audio/pcm`, the speech returned is `audio/pcm` at 16
      KHz in 16-bit, little-endian format.

    - The following are the accepted values:

      - audio/mpeg

      - audio/ogg

      - audio/pcm (16 KHz)

      - audio/* (defaults to mpeg)

      - text/plain; charset=utf-8
    """

    input_stream: StreamingBlob = b""
    """
    User input in PCM or Opus audio format or text format as described in
    the `requestContentType` parameter.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_RECOGNIZE_UTTERANCE_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.bot_id is not None:
            serializer.write_string(
                _SCHEMA_RECOGNIZE_UTTERANCE_INPUT.members["botId"], self.bot_id
            )

        if self.bot_alias_id is not None:
            serializer.write_string(
                _SCHEMA_RECOGNIZE_UTTERANCE_INPUT.members["botAliasId"],
                self.bot_alias_id,
            )

        if self.locale_id is not None:
            serializer.write_string(
                _SCHEMA_RECOGNIZE_UTTERANCE_INPUT.members["localeId"], self.locale_id
            )

        if self.session_id is not None:
            serializer.write_string(
                _SCHEMA_RECOGNIZE_UTTERANCE_INPUT.members["sessionId"], self.session_id
            )

        if self.session_state is not None:
            serializer.write_string(
                _SCHEMA_RECOGNIZE_UTTERANCE_INPUT.members["sessionState"],
                self.session_state,
            )

        if self.request_attributes is not None:
            serializer.write_string(
                _SCHEMA_RECOGNIZE_UTTERANCE_INPUT.members["requestAttributes"],
                self.request_attributes,
            )

        if self.request_content_type is not None:
            serializer.write_string(
                _SCHEMA_RECOGNIZE_UTTERANCE_INPUT.members["requestContentType"],
                self.request_content_type,
            )

        if self.response_content_type is not None:
            serializer.write_string(
                _SCHEMA_RECOGNIZE_UTTERANCE_INPUT.members["responseContentType"],
                self.response_content_type,
            )

        if self.input_stream is not None:
            serializer.write_data_stream(
                _SCHEMA_RECOGNIZE_UTTERANCE_INPUT.members["inputStream"],
                self.input_stream,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["bot_id"] = de.read_string(
                        _SCHEMA_RECOGNIZE_UTTERANCE_INPUT.members["botId"]
                    )

                case 1:
                    kwargs["bot_alias_id"] = de.read_string(
                        _SCHEMA_RECOGNIZE_UTTERANCE_INPUT.members["botAliasId"]
                    )

                case 2:
                    kwargs["locale_id"] = de.read_string(
                        _SCHEMA_RECOGNIZE_UTTERANCE_INPUT.members["localeId"]
                    )

                case 3:
                    kwargs["session_id"] = de.read_string(
                        _SCHEMA_RECOGNIZE_UTTERANCE_INPUT.members["sessionId"]
                    )

                case 4:
                    kwargs["session_state"] = de.read_string(
                        _SCHEMA_RECOGNIZE_UTTERANCE_INPUT.members["sessionState"]
                    )

                case 5:
                    kwargs["request_attributes"] = de.read_string(
                        _SCHEMA_RECOGNIZE_UTTERANCE_INPUT.members["requestAttributes"]
                    )

                case 6:
                    kwargs["request_content_type"] = de.read_string(
                        _SCHEMA_RECOGNIZE_UTTERANCE_INPUT.members["requestContentType"]
                    )

                case 7:
                    kwargs["response_content_type"] = de.read_string(
                        _SCHEMA_RECOGNIZE_UTTERANCE_INPUT.members["responseContentType"]
                    )

                case 8:
                    kwargs["input_stream"] = de.read_data_stream(
                        _SCHEMA_RECOGNIZE_UTTERANCE_INPUT.members["inputStream"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_RECOGNIZE_UTTERANCE_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class RecognizeUtteranceOutput:
    """Dataclass for RecognizeUtteranceOutput structure."""

    input_mode: str | None = None
    """
    Indicates whether the input mode to the operation was text, speech, or
    from a touch-tone keypad.
    """

    content_type: str | None = None
    """Content type as specified in the `responseContentType` in the request."""

    messages: str | None = None
    """
    A list of messages that were last sent to the user. The messages are
    ordered based on the order that you returned the messages from your
    Lambda function or the order that the messages are defined in the bot.

    The `messages` field is compressed with gzip and then base64 encoded.
    Before you can use the contents of the field, you must decode and
    decompress the contents. See the example for a simple function to decode
    and decompress the contents.
    """

    interpretations: str | None = None
    """
    A list of intents that Amazon Lex V2 determined might satisfy the
    user's utterance.

    Each interpretation includes the intent, a score that indicates how
    confident Amazon Lex V2 is that the interpretation is the correct one,
    and an optional sentiment response that indicates the sentiment
    expressed in the utterance.

    The `interpretations` field is compressed with gzip and then base64
    encoded. Before you can use the contents of the field, you must decode
    and decompress the contents. See the example for a simple function to
    decode and decompress the contents.
    """

    session_state: str | None = None
    """
    Represents the current state of the dialog between the user and the bot.

    Use this to determine the progress of the conversation and what the next
    action might be.

    The `sessionState` field is compressed with gzip and then base64
    encoded. Before you can use the contents of the field, you must decode
    and decompress the contents. See the example for a simple function to
    decode and decompress the contents.
    """

    request_attributes: str | None = None
    """
    The attributes sent in the request.

    The `requestAttributes` field is compressed with gzip and then base64
    encoded. Before you can use the contents of the field, you must decode
    and decompress the contents.
    """

    session_id: str | None = None
    """The identifier of the session in use."""

    input_transcript: str | None = None
    """
    The text used to process the request.

    If the input was an audio stream, the `inputTranscript` field contains
    the text extracted from the audio stream. This is the text that is
    actually processed to recognize intents and slot values. You can use
    this information to determine if Amazon Lex V2 is correctly processing
    the audio that you send.

    The `inputTranscript` field is compressed with gzip and then base64
    encoded. Before you can use the contents of the field, you must decode
    and decompress the contents. See the example for a simple function to
    decode and decompress the contents.
    """

    audio_stream: StreamingBlob = b""
    """
    The prompt or statement to send to the user. This is based on the bot
    configuration and context. For example, if Amazon Lex V2 did not
    understand the user intent, it sends the `clarificationPrompt`
    configured for the bot. If the intent requires confirmation before
    taking the fulfillment action, it sends the `confirmationPrompt`.
    Another example: Suppose that the Lambda function successfully fulfilled
    the intent, and sent a message to convey to the user. Then Amazon Lex V2
    sends that message in the response.
    """

    recognized_bot_member: str | None = None
    """The bot member that recognized the utterance."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_RECOGNIZE_UTTERANCE_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.input_mode is not None:
            serializer.write_string(
                _SCHEMA_RECOGNIZE_UTTERANCE_OUTPUT.members["inputMode"], self.input_mode
            )

        if self.content_type is not None:
            serializer.write_string(
                _SCHEMA_RECOGNIZE_UTTERANCE_OUTPUT.members["contentType"],
                self.content_type,
            )

        if self.messages is not None:
            serializer.write_string(
                _SCHEMA_RECOGNIZE_UTTERANCE_OUTPUT.members["messages"], self.messages
            )

        if self.interpretations is not None:
            serializer.write_string(
                _SCHEMA_RECOGNIZE_UTTERANCE_OUTPUT.members["interpretations"],
                self.interpretations,
            )

        if self.session_state is not None:
            serializer.write_string(
                _SCHEMA_RECOGNIZE_UTTERANCE_OUTPUT.members["sessionState"],
                self.session_state,
            )

        if self.request_attributes is not None:
            serializer.write_string(
                _SCHEMA_RECOGNIZE_UTTERANCE_OUTPUT.members["requestAttributes"],
                self.request_attributes,
            )

        if self.session_id is not None:
            serializer.write_string(
                _SCHEMA_RECOGNIZE_UTTERANCE_OUTPUT.members["sessionId"], self.session_id
            )

        if self.input_transcript is not None:
            serializer.write_string(
                _SCHEMA_RECOGNIZE_UTTERANCE_OUTPUT.members["inputTranscript"],
                self.input_transcript,
            )

        serializer.write_data_stream(
            _SCHEMA_RECOGNIZE_UTTERANCE_OUTPUT.members["audioStream"], self.audio_stream
        )
        if self.recognized_bot_member is not None:
            serializer.write_string(
                _SCHEMA_RECOGNIZE_UTTERANCE_OUTPUT.members["recognizedBotMember"],
                self.recognized_bot_member,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["input_mode"] = de.read_string(
                        _SCHEMA_RECOGNIZE_UTTERANCE_OUTPUT.members["inputMode"]
                    )

                case 1:
                    kwargs["content_type"] = de.read_string(
                        _SCHEMA_RECOGNIZE_UTTERANCE_OUTPUT.members["contentType"]
                    )

                case 2:
                    kwargs["messages"] = de.read_string(
                        _SCHEMA_RECOGNIZE_UTTERANCE_OUTPUT.members["messages"]
                    )

                case 3:
                    kwargs["interpretations"] = de.read_string(
                        _SCHEMA_RECOGNIZE_UTTERANCE_OUTPUT.members["interpretations"]
                    )

                case 4:
                    kwargs["session_state"] = de.read_string(
                        _SCHEMA_RECOGNIZE_UTTERANCE_OUTPUT.members["sessionState"]
                    )

                case 5:
                    kwargs["request_attributes"] = de.read_string(
                        _SCHEMA_RECOGNIZE_UTTERANCE_OUTPUT.members["requestAttributes"]
                    )

                case 6:
                    kwargs["session_id"] = de.read_string(
                        _SCHEMA_RECOGNIZE_UTTERANCE_OUTPUT.members["sessionId"]
                    )

                case 7:
                    kwargs["input_transcript"] = de.read_string(
                        _SCHEMA_RECOGNIZE_UTTERANCE_OUTPUT.members["inputTranscript"]
                    )

                case 8:
                    kwargs["audio_stream"] = de.read_data_stream(
                        _SCHEMA_RECOGNIZE_UTTERANCE_OUTPUT.members["audioStream"]
                    )

                case 9:
                    kwargs["recognized_bot_member"] = de.read_string(
                        _SCHEMA_RECOGNIZE_UTTERANCE_OUTPUT.members[
                            "recognizedBotMember"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_RECOGNIZE_UTTERANCE_OUTPUT, consumer=_consumer)
        return kwargs


RECOGNIZE_UTTERANCE = APIOperation(
    input=RecognizeUtteranceInput,
    output=RecognizeUtteranceOutput,
    schema=_SCHEMA_RECOGNIZE_UTTERANCE,
    input_schema=_SCHEMA_RECOGNIZE_UTTERANCE_INPUT,
    output_schema=_SCHEMA_RECOGNIZE_UTTERANCE_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID(
                "com.amazonaws.lexruntimev2#AccessDeniedException"
            ): AccessDeniedException,
            ShapeID(
                "com.amazonaws.lexruntimev2#BadGatewayException"
            ): BadGatewayException,
            ShapeID("com.amazonaws.lexruntimev2#ConflictException"): ConflictException,
            ShapeID(
                "com.amazonaws.lexruntimev2#DependencyFailedException"
            ): DependencyFailedException,
            ShapeID(
                "com.amazonaws.lexruntimev2#InternalServerException"
            ): InternalServerException,
            ShapeID(
                "com.amazonaws.lexruntimev2#ResourceNotFoundException"
            ): ResourceNotFoundException,
            ShapeID(
                "com.amazonaws.lexruntimev2#ThrottlingException"
            ): ThrottlingException,
            ShapeID(
                "com.amazonaws.lexruntimev2#ValidationException"
            ): ValidationException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_ACCESS_DENIED_EXCEPTION,
        _SCHEMA_BAD_GATEWAY_EXCEPTION,
        _SCHEMA_CONFLICT_EXCEPTION,
        _SCHEMA_DEPENDENCY_FAILED_EXCEPTION,
        _SCHEMA_INTERNAL_SERVER_EXCEPTION,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
        _SCHEMA_THROTTLING_EXCEPTION,
        _SCHEMA_VALIDATION_EXCEPTION,
    ],
)


class ConversationMode(UnknownEnumMixin, StrEnum):
    AUDIO = "AUDIO"
    TEXT = "TEXT"


@dataclass(kw_only=True)
class DisconnectionEvent:
    """
    A notification from the client that it is disconnecting from Amazon Lex
    V2. Sending a `DisconnectionEvent` event is optional, but can help
    identify a conversation in logs.
    """

    event_id: str | None = None
    """
    A unique identifier that your application assigns to the event. You can
    use this to identify events in logs.
    """

    client_timestamp_millis: int = 0
    """
    A timestamp set by the client of the date and time that the event was
    sent to Amazon Lex V2.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DISCONNECTION_EVENT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.event_id is not None:
            serializer.write_string(
                _SCHEMA_DISCONNECTION_EVENT.members["eventId"], self.event_id
            )

        serializer.write_long(
            _SCHEMA_DISCONNECTION_EVENT.members["clientTimestampMillis"],
            self.client_timestamp_millis,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["event_id"] = de.read_string(
                        _SCHEMA_DISCONNECTION_EVENT.members["eventId"]
                    )

                case 1:
                    kwargs["client_timestamp_millis"] = de.read_long(
                        _SCHEMA_DISCONNECTION_EVENT.members["clientTimestampMillis"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_DISCONNECTION_EVENT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class DTMFInputEvent:
    """
    A DTMF character sent from the client application. DTMF characters are
    typically sent from a phone keypad to represent numbers. For example,
    you can have Amazon Lex V2 process a credit card number input from a
    phone.
    """

    input_character: str = field(repr=False)
    """
    The DTMF character that the user pressed. The allowed characters are A -
    D, 0 - 9, # and *.
    """

    event_id: str | None = None
    """
    A unique identifier that your application assigns to the event. You can
    use this to identify events in logs.
    """

    client_timestamp_millis: int = 0
    """
    A timestamp set by the client of the date and time that the event was
    sent to Amazon Lex V2.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DTMF_INPUT_EVENT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(
            _SCHEMA_DTMF_INPUT_EVENT.members["inputCharacter"], self.input_character
        )
        if self.event_id is not None:
            serializer.write_string(
                _SCHEMA_DTMF_INPUT_EVENT.members["eventId"], self.event_id
            )

        serializer.write_long(
            _SCHEMA_DTMF_INPUT_EVENT.members["clientTimestampMillis"],
            self.client_timestamp_millis,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["input_character"] = de.read_string(
                        _SCHEMA_DTMF_INPUT_EVENT.members["inputCharacter"]
                    )

                case 1:
                    kwargs["event_id"] = de.read_string(
                        _SCHEMA_DTMF_INPUT_EVENT.members["eventId"]
                    )

                case 2:
                    kwargs["client_timestamp_millis"] = de.read_long(
                        _SCHEMA_DTMF_INPUT_EVENT.members["clientTimestampMillis"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_DTMF_INPUT_EVENT, consumer=_consumer)
        if "input_character" not in kwargs:
            kwargs["input_character"] = ""
        return kwargs


@dataclass(kw_only=True)
class PlaybackCompletionEvent:
    """
    Event sent from the client application to Amazon Lex V2 to indicate that
    playback of audio is complete and that Amazon Lex V2 should start
    processing the user's input.
    """

    event_id: str | None = None
    """
    A unique identifier that your application assigns to the event. You can
    use this to identify events in logs.
    """

    client_timestamp_millis: int = 0
    """
    A timestamp set by the client of the date and time that the event was
    sent to Amazon Lex V2.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_PLAYBACK_COMPLETION_EVENT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.event_id is not None:
            serializer.write_string(
                _SCHEMA_PLAYBACK_COMPLETION_EVENT.members["eventId"], self.event_id
            )

        serializer.write_long(
            _SCHEMA_PLAYBACK_COMPLETION_EVENT.members["clientTimestampMillis"],
            self.client_timestamp_millis,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["event_id"] = de.read_string(
                        _SCHEMA_PLAYBACK_COMPLETION_EVENT.members["eventId"]
                    )

                case 1:
                    kwargs["client_timestamp_millis"] = de.read_long(
                        _SCHEMA_PLAYBACK_COMPLETION_EVENT.members[
                            "clientTimestampMillis"
                        ]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_PLAYBACK_COMPLETION_EVENT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class TextInputEvent:
    """
    The event sent from your client application to Amazon Lex V2 with text
    input from the user.
    """

    text: str = field(repr=False)
    """
    The text from the user. Amazon Lex V2 processes this as a complete
    statement.
    """

    event_id: str | None = None
    """
    A unique identifier that your application assigns to the event. You can
    use this to identify events in logs.
    """

    client_timestamp_millis: int = 0
    """
    A timestamp set by the client of the date and time that the event was
    sent to Amazon Lex V2.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_TEXT_INPUT_EVENT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(_SCHEMA_TEXT_INPUT_EVENT.members["text"], self.text)
        if self.event_id is not None:
            serializer.write_string(
                _SCHEMA_TEXT_INPUT_EVENT.members["eventId"], self.event_id
            )

        serializer.write_long(
            _SCHEMA_TEXT_INPUT_EVENT.members["clientTimestampMillis"],
            self.client_timestamp_millis,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["text"] = de.read_string(
                        _SCHEMA_TEXT_INPUT_EVENT.members["text"]
                    )

                case 1:
                    kwargs["event_id"] = de.read_string(
                        _SCHEMA_TEXT_INPUT_EVENT.members["eventId"]
                    )

                case 2:
                    kwargs["client_timestamp_millis"] = de.read_long(
                        _SCHEMA_TEXT_INPUT_EVENT.members["clientTimestampMillis"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_TEXT_INPUT_EVENT, consumer=_consumer)
        if "text" not in kwargs:
            kwargs["text"] = ""
        return kwargs


@dataclass(kw_only=True)
class HeartbeatEvent:
    """
    Event that Amazon Lex V2 sends to indicate that the stream is still open
    between the client application and Amazon Lex V2
    """

    event_id: str | None = None
    """
    A unique identifier of the event sent by Amazon Lex V2. The identifier
    is in the form `RESPONSE-N`, where N is a number starting with one and
    incremented for each event sent by Amazon Lex V2 in the current session.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_HEARTBEAT_EVENT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.event_id is not None:
            serializer.write_string(
                _SCHEMA_HEARTBEAT_EVENT.members["eventId"], self.event_id
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["event_id"] = de.read_string(
                        _SCHEMA_HEARTBEAT_EVENT.members["eventId"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_HEARTBEAT_EVENT, consumer=_consumer)
        return kwargs


class InputMode(UnknownEnumMixin, StrEnum):
    TEXT = "Text"
    SPEECH = "Speech"
    DTMF = "DTMF"


class PlaybackInterruptionReason(UnknownEnumMixin, StrEnum):
    DTMF_START_DETECTED = "DTMF_START_DETECTED"
    TEXT_DETECTED = "TEXT_DETECTED"
    VOICE_START_DETECTED = "VOICE_START_DETECTED"


@dataclass(kw_only=True)
class PlaybackInterruptionEvent:
    """
    Event sent from Amazon Lex V2 to indicate to the client application
    should stop playback of audio. For example, if the client is playing a
    prompt that asks for the user's telephone number, the user might start
    to say the phone number before the prompt is complete. Amazon Lex V2
    sends this event to the client application to indicate that the user is
    responding and that Amazon Lex V2 is processing their input.
    """

    event_reason: str | None = None
    """Indicates the type of user input that Amazon Lex V2 detected."""

    caused_by_event_id: str | None = None
    """
    The identifier of the event that contained the audio, DTMF, or text that
    caused the interruption.
    """

    event_id: str | None = None
    """
    A unique identifier of the event sent by Amazon Lex V2. The identifier
    is in the form `RESPONSE-N`, where N is a number starting with one and
    incremented for each event sent by Amazon Lex V2 in the current session.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_PLAYBACK_INTERRUPTION_EVENT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.event_reason is not None:
            serializer.write_string(
                _SCHEMA_PLAYBACK_INTERRUPTION_EVENT.members["eventReason"],
                self.event_reason,
            )

        if self.caused_by_event_id is not None:
            serializer.write_string(
                _SCHEMA_PLAYBACK_INTERRUPTION_EVENT.members["causedByEventId"],
                self.caused_by_event_id,
            )

        if self.event_id is not None:
            serializer.write_string(
                _SCHEMA_PLAYBACK_INTERRUPTION_EVENT.members["eventId"], self.event_id
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["event_reason"] = PlaybackInterruptionReason(
                        de.read_string(
                            _SCHEMA_PLAYBACK_INTERRUPTION_EVENT.members["eventReason"]
                        )
                    )

                case 1:
                    kwargs["caused_by_event_id"] = de.read_string(
                        _SCHEMA_PLAYBACK_INTERRUPTION_EVENT.members["causedByEventId"]
                    )

                case 2:
                    kwargs["event_id"] = de.read_string(
                        _SCHEMA_PLAYBACK_INTERRUPTION_EVENT.members["eventId"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(
            _SCHEMA_PLAYBACK_INTERRUPTION_EVENT, consumer=_consumer
        )
        return kwargs


@dataclass(kw_only=True)
class TextResponseEvent:
    """
    The event sent from Amazon Lex V2 to your application with text to
    present to the user.
    """

    messages: list[Message] | None = None
    """
    A list of messages to send to the user. Messages are ordered based on
    the order that you returned the messages from your Lambda function or
    the order that the messages are defined in the bot.
    """

    event_id: str | None = None
    """
    A unique identifier of the event sent by Amazon Lex V2. The identifier
    is in the form `RESPONSE-N`, where N is a number starting with one and
    incremented for each event sent by Amazon Lex V2 in the current session.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_TEXT_RESPONSE_EVENT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.messages is not None:
            _serialize_messages(
                serializer,
                _SCHEMA_TEXT_RESPONSE_EVENT.members["messages"],
                self.messages,
            )

        if self.event_id is not None:
            serializer.write_string(
                _SCHEMA_TEXT_RESPONSE_EVENT.members["eventId"], self.event_id
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["messages"] = _deserialize_messages(
                        de, _SCHEMA_TEXT_RESPONSE_EVENT.members["messages"]
                    )

                case 1:
                    kwargs["event_id"] = de.read_string(
                        _SCHEMA_TEXT_RESPONSE_EVENT.members["eventId"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_TEXT_RESPONSE_EVENT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class TranscriptEvent:
    """
    Event sent from Amazon Lex V2 to your client application that contains a
    transcript of voice audio.
    """

    transcript: str | None = None
    """The transcript of the voice audio from the user."""

    event_id: str | None = None
    """
    A unique identifier of the event sent by Amazon Lex V2. The identifier
    is in the form `RESPONSE-N`, where N is a number starting with one and
    incremented for each event sent by Amazon Lex V2 in the current session.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_TRANSCRIPT_EVENT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.transcript is not None:
            serializer.write_string(
                _SCHEMA_TRANSCRIPT_EVENT.members["transcript"], self.transcript
            )

        if self.event_id is not None:
            serializer.write_string(
                _SCHEMA_TRANSCRIPT_EVENT.members["eventId"], self.event_id
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["transcript"] = de.read_string(
                        _SCHEMA_TRANSCRIPT_EVENT.members["transcript"]
                    )

                case 1:
                    kwargs["event_id"] = de.read_string(
                        _SCHEMA_TRANSCRIPT_EVENT.members["eventId"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_TRANSCRIPT_EVENT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class ElicitSubSlot:
    """
    The specific constituent sub slot of the composite slot to elicit in
    dialog action.
    """

    name: str
    """The name of the slot that should be elicited from the user."""

    sub_slot_to_elicit: "ElicitSubSlot | None" = None
    """The field is not supported."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_ELICIT_SUB_SLOT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(_SCHEMA_ELICIT_SUB_SLOT.members["name"], self.name)
        if self.sub_slot_to_elicit is not None:
            serializer.write_struct(
                _SCHEMA_ELICIT_SUB_SLOT.members["subSlotToElicit"],
                self.sub_slot_to_elicit,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["name"] = de.read_string(
                        _SCHEMA_ELICIT_SUB_SLOT.members["name"]
                    )

                case 1:
                    kwargs["sub_slot_to_elicit"] = ElicitSubSlot.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_ELICIT_SUB_SLOT, consumer=_consumer)
        if "name" not in kwargs:
            kwargs["name"] = ""
        return kwargs


@dataclass(kw_only=True)
class DialogAction:
    """The next action that Amazon Lex V2 should take."""

    type: str
    """
    The next action that the bot should take in its interaction with the
    user. The following values are possible:

    - `Close` -- Indicates that there will not be a response from the user.
      For example, the statement \"Your order has been placed\" does not
      require a response.

    - `ConfirmIntent` -- The next action is asking the user if the intent is
      complete and ready to be fulfilled. This is a yes/no question such as
      \"Place the order?\"

    - `Delegate` -- The next action is determined by Amazon Lex V2.

    - `ElicitIntent` -- The next action is to elicit an intent from the
      user.

    - `ElicitSlot` -- The next action is to elicit a slot value from the
      user.
    """

    slot_to_elicit: str | None = None
    """The name of the slot that should be elicited from the user."""

    slot_elicitation_style: str | None = None
    """
    Configures the slot to use spell-by-letter or spell-by-word style. When
    you use a style on a slot, users can spell out their input to make it
    clear to your bot.

    - Spell by letter - \"b\" \"o\" \"b\"

    - Spell by word - \"b as in boy\" \"o as in oscar\" \"b as in boy\"

    For more information, see [Using spelling to enter slot
    values](https://docs.aws.amazon.com/lexv2/latest/dg/spelling-styles.html)
    .
    """

    sub_slot_to_elicit: "ElicitSubSlot | None" = None
    """
    The name of the constituent sub slot of the composite slot specified in
    slotToElicit that should be elicited from the user.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_DIALOG_ACTION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(_SCHEMA_DIALOG_ACTION.members["type"], self.type)
        if self.slot_to_elicit is not None:
            serializer.write_string(
                _SCHEMA_DIALOG_ACTION.members["slotToElicit"], self.slot_to_elicit
            )

        if self.slot_elicitation_style is not None:
            serializer.write_string(
                _SCHEMA_DIALOG_ACTION.members["slotElicitationStyle"],
                self.slot_elicitation_style,
            )

        if self.sub_slot_to_elicit is not None:
            serializer.write_struct(
                _SCHEMA_DIALOG_ACTION.members["subSlotToElicit"],
                self.sub_slot_to_elicit,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["type"] = DialogActionType(
                        de.read_string(_SCHEMA_DIALOG_ACTION.members["type"])
                    )

                case 1:
                    kwargs["slot_to_elicit"] = de.read_string(
                        _SCHEMA_DIALOG_ACTION.members["slotToElicit"]
                    )

                case 2:
                    kwargs["slot_elicitation_style"] = StyleType(
                        de.read_string(
                            _SCHEMA_DIALOG_ACTION.members["slotElicitationStyle"]
                        )
                    )

                case 3:
                    kwargs["sub_slot_to_elicit"] = ElicitSubSlot.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_DIALOG_ACTION, consumer=_consumer)
        if "type" not in kwargs:
            kwargs["type"] = DialogActionType._corrected("")
        return kwargs


@dataclass(kw_only=True)
class RuntimeHintDetails:
    """
    Provides an array of phrases that should be given preference when
    resolving values for a slot.
    """

    runtime_hint_values: list[RuntimeHintValue] | None = None
    """
    One or more strings that Amazon Lex V2 should look for in the input to
    the bot. Each phrase is given preference when deciding on slot values.
    """

    sub_slot_hints: "dict[str, RuntimeHintDetails] | None" = None
    """
    A map of constituent sub slot names inside a composite slot in the
    intent and the phrases that should be added for each sub slot. Inside
    each composite slot hints, this structure provides a mechanism to add
    granular sub slot phrases. Only sub slot hints are supported for
    composite slots. The intent name, composite slot name and the
    constituent sub slot names must exist.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_RUNTIME_HINT_DETAILS, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.runtime_hint_values is not None:
            _serialize_runtime_hint_values_list(
                serializer,
                _SCHEMA_RUNTIME_HINT_DETAILS.members["runtimeHintValues"],
                self.runtime_hint_values,
            )

        if self.sub_slot_hints is not None:
            _serialize_slot_hints_slot_map(
                serializer,
                _SCHEMA_RUNTIME_HINT_DETAILS.members["subSlotHints"],
                self.sub_slot_hints,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["runtime_hint_values"] = (
                        _deserialize_runtime_hint_values_list(
                            de,
                            _SCHEMA_RUNTIME_HINT_DETAILS.members["runtimeHintValues"],
                        )
                    )

                case 1:
                    kwargs["sub_slot_hints"] = _deserialize_slot_hints_slot_map(
                        de, _SCHEMA_RUNTIME_HINT_DETAILS.members["subSlotHints"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_RUNTIME_HINT_DETAILS, consumer=_consumer)
        return kwargs


def _serialize_slot_hints_slot_map(
    serializer: ShapeSerializer, schema: Schema, value: dict[str, RuntimeHintDetails]
) -> None:
    with serializer.begin_map(schema, len(value)) as m:
        value_schema = schema.members["value"]
        for k, v in value.items():
            m.entry(k, lambda vs: vs.write_struct(value_schema, v))


def _deserialize_slot_hints_slot_map(
    deserializer: ShapeDeserializer, schema: Schema
) -> dict[str, RuntimeHintDetails]:
    result: dict[str, RuntimeHintDetails] = {}
    value_schema = schema.members["value"]

    def _read_value(k: str, d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result[k] = RuntimeHintDetails.deserialize(d)

    deserializer.read_map(schema, _read_value)
    return result


def _serialize_slot_hints_intent_map(
    serializer: ShapeSerializer,
    schema: Schema,
    value: dict[str, dict[str, RuntimeHintDetails]],
) -> None:
    with serializer.begin_map(schema, len(value)) as m:
        value_schema = schema.members["value"]
        for k, v in value.items():
            m.entry(k, lambda vs: _serialize_slot_hints_slot_map(vs, value_schema, v))


def _deserialize_slot_hints_intent_map(
    deserializer: ShapeDeserializer, schema: Schema
) -> dict[str, dict[str, RuntimeHintDetails]]:
    result: dict[str, dict[str, RuntimeHintDetails]] = {}
    value_schema = schema.members["value"]

    def _read_value(k: str, d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result[k] = _deserialize_slot_hints_slot_map(d, value_schema)

    deserializer.read_map(schema, _read_value)
    return result


@dataclass(kw_only=True)
class RuntimeHints:
    """
    You can provide Amazon Lex V2 with hints to the phrases that a customer
    is likely to use for a slot. When a slot with hints is resolved, the
    phrases in the runtime hints are preferred in the resolution. You can
    provide hints for a maximum of 100 intents. You can provide a maximum of
    100 slots.

    Before you can use runtime hints with an existing bot, you must first
    rebuild the bot.

    For more information, see [Using runtime hints to improve recognition of
    slot
    values](https://docs.aws.amazon.com/lexv2/latest/dg/using-hints.html).
    """

    slot_hints: "dict[str, dict[str, RuntimeHintDetails]] | None" = None
    """
    A list of the slots in the intent that should have runtime hints added,
    and the phrases that should be added for each slot.

    The first level of the `slotHints` map is the name of the intent. The
    second level is the name of the slot within the intent. For more
    information, see [Using hints to improve
    accuracy](https://docs.aws.amazon.com/lexv2/latest/dg/using-hints.html).

    The intent name and slot name must exist.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_RUNTIME_HINTS, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.slot_hints is not None:
            _serialize_slot_hints_intent_map(
                serializer, _SCHEMA_RUNTIME_HINTS.members["slotHints"], self.slot_hints
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["slot_hints"] = _deserialize_slot_hints_intent_map(
                        de, _SCHEMA_RUNTIME_HINTS.members["slotHints"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_RUNTIME_HINTS, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class Slot:
    """A value that Amazon Lex V2 uses to fulfill an intent."""

    value: Value | None = None
    """The current value of the slot."""

    shape: str | None = None
    """
    When the `shape` value is `List`, it indicates that the `values` field
    contains a list of slot values. When the value is `Scalar`, it indicates
    that the `value` field contains a single value.
    """

    values: "list[Slot] | None" = None
    """
    A list of one or more values that the user provided for the slot. For
    example, if a for a slot that elicits pizza toppings, the values might
    be \"pepperoni\" and \"pineapple.\"
    """

    sub_slots: "dict[str, Slot] | None" = None
    """The constituent sub slots of a composite slot."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_SLOT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.value is not None:
            serializer.write_struct(_SCHEMA_SLOT.members["value"], self.value)

        if self.shape is not None:
            serializer.write_string(_SCHEMA_SLOT.members["shape"], self.shape)

        if self.values is not None:
            _serialize_values(serializer, _SCHEMA_SLOT.members["values"], self.values)

        if self.sub_slots is not None:
            _serialize_slots(
                serializer, _SCHEMA_SLOT.members["subSlots"], self.sub_slots
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["value"] = Value.deserialize(de)

                case 1:
                    kwargs["shape"] = Shape(
                        de.read_string(_SCHEMA_SLOT.members["shape"])
                    )

                case 2:
                    kwargs["values"] = _deserialize_values(
                        de, _SCHEMA_SLOT.members["values"]
                    )

                case 3:
                    kwargs["sub_slots"] = _deserialize_slots(
                        de, _SCHEMA_SLOT.members["subSlots"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_SLOT, consumer=_consumer)
        return kwargs


def _serialize_slots(
    serializer: ShapeSerializer, schema: Schema, value: dict[str, Slot]
) -> None:
    with serializer.begin_map(schema, len(value)) as m:
        value_schema = schema.members["value"]
        for k, v in value.items():
            m.entry(k, lambda vs: vs.write_struct(value_schema, v))


def _deserialize_slots(
    deserializer: ShapeDeserializer, schema: Schema
) -> dict[str, Slot]:
    result: dict[str, Slot] = {}
    value_schema = schema.members["value"]

    def _read_value(k: str, d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result[k] = Slot.deserialize(d)

    deserializer.read_map(schema, _read_value)
    return result


def _serialize_values(
    serializer: ShapeSerializer, schema: Schema, value: list[Slot]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_values(deserializer: ShapeDeserializer, schema: Schema) -> list[Slot]:
    result: list[Slot] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(Slot.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class Intent:
    """The current intent that Amazon Lex V2 is attempting to fulfill."""

    name: str
    """The name of the intent."""

    slots: "dict[str, Slot] | None" = None
    """
    A map of all of the slots for the intent. The name of the slot maps to
    the value of the slot. If a slot has not been filled, the value is null.
    """

    state: str | None = None
    """
    Indicates the fulfillment state for the intent. The meanings of each
    value are as follows:

    - `Failed` -- The bot failed to fulfill the intent.

    - `Fulfilled` -- The bot has completed fulfillment of the intent.

    - `FulfillmentInProgress` -- The bot is in the middle of fulfilling the
      intent.

    - `InProgress` -- The bot is in the middle of eliciting the slot values
      that are necessary to fulfill the intent.

    - `ReadyForFulfillment` -- The bot has elicited all the slot values for
      the intent and is ready to fulfill the intent.

    - `Waiting` -- The bot is waiting for a response from the user (limited
      to streaming conversations).
    """

    confirmation_state: str | None = None
    """
    Indicates whether the intent has been `Confirmed`, `Denied`, or `None`
    if the confirmation stage has not yet been reached.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_INTENT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_string(_SCHEMA_INTENT.members["name"], self.name)
        if self.slots is not None:
            _serialize_slots(serializer, _SCHEMA_INTENT.members["slots"], self.slots)

        if self.state is not None:
            serializer.write_string(_SCHEMA_INTENT.members["state"], self.state)

        if self.confirmation_state is not None:
            serializer.write_string(
                _SCHEMA_INTENT.members["confirmationState"], self.confirmation_state
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["name"] = de.read_string(_SCHEMA_INTENT.members["name"])

                case 1:
                    kwargs["slots"] = _deserialize_slots(
                        de, _SCHEMA_INTENT.members["slots"]
                    )

                case 2:
                    kwargs["state"] = IntentState(
                        de.read_string(_SCHEMA_INTENT.members["state"])
                    )

                case 3:
                    kwargs["confirmation_state"] = ConfirmationState(
                        de.read_string(_SCHEMA_INTENT.members["confirmationState"])
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_INTENT, consumer=_consumer)
        if "name" not in kwargs:
            kwargs["name"] = ""
        return kwargs


@dataclass(kw_only=True)
class Interpretation:
    """
    An object containing information about an intent that Amazon Lex V2
    determined might satisfy the user's utterance. The intents are ordered
    by the confidence score.
    """

    nlu_confidence: ConfidenceScore | None = None
    """
    Determines the threshold where Amazon Lex V2 will insert the
    `AMAZON.FallbackIntent`, `AMAZON.KendraSearchIntent`, or both when
    returning alternative intents in a response. `AMAZON.FallbackIntent` and
    `AMAZON.KendraSearchIntent` are only inserted if they are configured for
    the bot.
    """

    sentiment_response: SentimentResponse | None = None
    """
    The sentiment expressed in an utterance.

    When the bot is configured to send utterances to Amazon Comprehend for
    sentiment analysis, this field contains the result of the analysis.
    """

    intent: "Intent | None" = None
    """
    A list of intents that might satisfy the user's utterance. The intents
    are ordered by the confidence score.
    """

    interpretation_source: str | None = None
    """Specifies the service that interpreted the input."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_INTERPRETATION, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.nlu_confidence is not None:
            serializer.write_struct(
                _SCHEMA_INTERPRETATION.members["nluConfidence"], self.nlu_confidence
            )

        if self.sentiment_response is not None:
            serializer.write_struct(
                _SCHEMA_INTERPRETATION.members["sentimentResponse"],
                self.sentiment_response,
            )

        if self.intent is not None:
            serializer.write_struct(
                _SCHEMA_INTERPRETATION.members["intent"], self.intent
            )

        if self.interpretation_source is not None:
            serializer.write_string(
                _SCHEMA_INTERPRETATION.members["interpretationSource"],
                self.interpretation_source,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["nlu_confidence"] = ConfidenceScore.deserialize(de)

                case 1:
                    kwargs["sentiment_response"] = SentimentResponse.deserialize(de)

                case 2:
                    kwargs["intent"] = Intent.deserialize(de)

                case 3:
                    kwargs["interpretation_source"] = InterpretationSource(
                        de.read_string(
                            _SCHEMA_INTERPRETATION.members["interpretationSource"]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_INTERPRETATION, consumer=_consumer)
        return kwargs


def _serialize_interpretations(
    serializer: ShapeSerializer, schema: Schema, value: list[Interpretation]
) -> None:
    member_schema = schema.members["member"]
    with serializer.begin_list(schema, len(value)) as ls:
        for e in value:
            ls.write_struct(member_schema, e)


def _deserialize_interpretations(
    deserializer: ShapeDeserializer, schema: Schema
) -> list[Interpretation]:
    result: list[Interpretation] = []

    def _read_value(d: ShapeDeserializer):
        if d.is_null():
            d.read_null()

        else:
            result.append(Interpretation.deserialize(d))

    deserializer.read_list(schema, _read_value)
    return result


@dataclass(kw_only=True)
class SessionState:
    """The state of the user's session with Amazon Lex V2."""

    dialog_action: "DialogAction | None" = None
    """
    The next step that Amazon Lex V2 should take in the conversation with a
    user.
    """

    intent: "Intent | None" = None
    """The active intent that Amazon Lex V2 is processing."""

    active_contexts: list[ActiveContext] | None = None
    """
    One or more contexts that indicate to Amazon Lex V2 the context of a
    request. When a context is active, Amazon Lex V2 considers intents with
    the matching context as a trigger as the next intent in a session.
    """

    session_attributes: dict[str, str] | None = None
    """
    Map of key/value pairs representing session-specific context
    information. It contains application information passed between Amazon
    Lex V2 and a client application.
    """

    originating_request_id: str | None = None
    """A unique identifier for a specific request."""

    runtime_hints: "RuntimeHints | None" = None
    """
    Hints for phrases that a customer is likely to use for a slot. Amazon
    Lex V2 uses the hints to help determine the correct value of a slot.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_SESSION_STATE, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.dialog_action is not None:
            serializer.write_struct(
                _SCHEMA_SESSION_STATE.members["dialogAction"], self.dialog_action
            )

        if self.intent is not None:
            serializer.write_struct(
                _SCHEMA_SESSION_STATE.members["intent"], self.intent
            )

        if self.active_contexts is not None:
            _serialize_active_contexts_list(
                serializer,
                _SCHEMA_SESSION_STATE.members["activeContexts"],
                self.active_contexts,
            )

        if self.session_attributes is not None:
            _serialize_string_map(
                serializer,
                _SCHEMA_SESSION_STATE.members["sessionAttributes"],
                self.session_attributes,
            )

        if self.originating_request_id is not None:
            serializer.write_string(
                _SCHEMA_SESSION_STATE.members["originatingRequestId"],
                self.originating_request_id,
            )

        if self.runtime_hints is not None:
            serializer.write_struct(
                _SCHEMA_SESSION_STATE.members["runtimeHints"], self.runtime_hints
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["dialog_action"] = DialogAction.deserialize(de)

                case 1:
                    kwargs["intent"] = Intent.deserialize(de)

                case 2:
                    kwargs["active_contexts"] = _deserialize_active_contexts_list(
                        de, _SCHEMA_SESSION_STATE.members["activeContexts"]
                    )

                case 3:
                    kwargs["session_attributes"] = _deserialize_string_map(
                        de, _SCHEMA_SESSION_STATE.members["sessionAttributes"]
                    )

                case 4:
                    kwargs["originating_request_id"] = de.read_string(
                        _SCHEMA_SESSION_STATE.members["originatingRequestId"]
                    )

                case 5:
                    kwargs["runtime_hints"] = RuntimeHints.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_SESSION_STATE, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class ConfigurationEvent:
    """
    The initial event sent from the application to Amazon Lex V2 to
    configure the conversation, including session and request attributes and
    the response content type.
    """

    response_content_type: str
    """
    The message that Amazon Lex V2 returns in the response can be either
    text or speech based on the `responseContentType` value.

    - If the value is `text/plain;charset=utf-8`, Amazon Lex V2 returns text
      in the response.

    - If the value begins with `audio/`, Amazon Lex V2 returns speech in the
      response. Amazon Lex V2 uses Amazon Polly to generate the speech using
      the configuration that you specified in the `requestContentType`
      parameter. For example, if you specify `audio/mpeg` as the value,
      Amazon Lex V2 returns speech in the MPEG format.

    - If the value is `audio/pcm`, the speech returned is audio/pcm in
      16-bit, little-endian format.

    - The following are the accepted values:

      - audio/mpeg

      - audio/ogg

      - audio/pcm

      - audio/* (defaults to mpeg)

      - text/plain; charset=utf-8
    """

    request_attributes: dict[str, str] | None = None
    """
    Request-specific information passed between the client application and
    Amazon Lex V2.

    The namespace `x-amz-lex:` is reserved for special attributes. Don't
    create any request attributes for prefix `x-amz-lex:`.
    """

    session_state: "SessionState | None" = None
    """The state of the user's session with Amazon Lex V2."""

    welcome_messages: list[Message] | None = None
    """
    A list of messages to send to the user.

    If you set the `welcomeMessage` field, you must also set the
    [`DialogAction`](https://docs.aws.amazon.com/lexv2/latest/dg/API_runtime_DialogAction.html)
    structure's
    [`type`](https://docs.aws.amazon.com/lexv2/latest/dg/API_runtime_DialogAction.html#lexv2-Type-runtime_DialogAction-type)
    field.
    """

    disable_playback: bool = False
    """
    Determines whether Amazon Lex V2 should send audio responses to the
    client application.

    Set this field to false when the client is operating in a playback mode
    where audio responses are played to the user. If the client isn't
    operating in playback mode, such as a text chat application, set this to
    true so that Amazon Lex V2 doesn't wait for the prompt to finish
    playing on the client.
    """

    event_id: str | None = None
    """
    A unique identifier that your application assigns to the event. You can
    use this to identify events in logs.
    """

    client_timestamp_millis: int = 0
    """
    A timestamp set by the client of the date and time that the event was
    sent to Amazon Lex V2.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_CONFIGURATION_EVENT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.request_attributes is not None:
            _serialize_string_map(
                serializer,
                _SCHEMA_CONFIGURATION_EVENT.members["requestAttributes"],
                self.request_attributes,
            )

        serializer.write_string(
            _SCHEMA_CONFIGURATION_EVENT.members["responseContentType"],
            self.response_content_type,
        )
        if self.session_state is not None:
            serializer.write_struct(
                _SCHEMA_CONFIGURATION_EVENT.members["sessionState"], self.session_state
            )

        if self.welcome_messages is not None:
            _serialize_messages(
                serializer,
                _SCHEMA_CONFIGURATION_EVENT.members["welcomeMessages"],
                self.welcome_messages,
            )

        serializer.write_boolean(
            _SCHEMA_CONFIGURATION_EVENT.members["disablePlayback"],
            self.disable_playback,
        )
        if self.event_id is not None:
            serializer.write_string(
                _SCHEMA_CONFIGURATION_EVENT.members["eventId"], self.event_id
            )

        serializer.write_long(
            _SCHEMA_CONFIGURATION_EVENT.members["clientTimestampMillis"],
            self.client_timestamp_millis,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["request_attributes"] = _deserialize_string_map(
                        de, _SCHEMA_CONFIGURATION_EVENT.members["requestAttributes"]
                    )

                case 1:
                    kwargs["response_content_type"] = de.read_string(
                        _SCHEMA_CONFIGURATION_EVENT.members["responseContentType"]
                    )

                case 2:
                    kwargs["session_state"] = SessionState.deserialize(de)

                case 3:
                    kwargs["welcome_messages"] = _deserialize_messages(
                        de, _SCHEMA_CONFIGURATION_EVENT.members["welcomeMessages"]
                    )

                case 4:
                    kwargs["disable_playback"] = de.read_boolean(
                        _SCHEMA_CONFIGURATION_EVENT.members["disablePlayback"]
                    )

                case 5:
                    kwargs["event_id"] = de.read_string(
                        _SCHEMA_CONFIGURATION_EVENT.members["eventId"]
                    )

                case 6:
                    kwargs["client_timestamp_millis"] = de.read_long(
                        _SCHEMA_CONFIGURATION_EVENT.members["clientTimestampMillis"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_CONFIGURATION_EVENT, consumer=_consumer)
        if "response_content_type" not in kwargs:
            kwargs["response_content_type"] = ""
        return kwargs


@dataclass(kw_only=True)
class PutSessionInput:
    """Dataclass for PutSessionInput structure."""

    bot_id: str | None = None
    """The identifier of the bot that receives the session data."""

    bot_alias_id: str | None = None
    """The alias identifier of the bot that receives the session data."""

    locale_id: str | None = None
    """The locale where the session is in use."""

    session_id: str | None = None
    """The identifier of the session that receives the session data."""

    messages: list[Message] | None = None
    """
    A list of messages to send to the user. Messages are sent in the order
    that they are defined in the list.
    """

    session_state: "SessionState | None" = None
    """
    Sets the state of the session with the user. You can use this to set the
    current intent, attributes, context, and dialog action. Use the dialog
    action to determine the next step that Amazon Lex V2 should use in the
    conversation with the user.
    """

    request_attributes: dict[str, str] | None = None
    """
    Request-specific information passed between Amazon Lex V2 and the client
    application.

    The namespace `x-amz-lex:` is reserved for special attributes. Don't
    create any request attributes with the prefix `x-amz-lex:`.
    """

    response_content_type: str | None = None
    """
    The message that Amazon Lex V2 returns in the response can be either
    text or speech depending on the value of this parameter.

    - If the value is `text/plain; charset=utf-8`, Amazon Lex V2 returns
      text in the response.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_PUT_SESSION_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.bot_id is not None:
            serializer.write_string(
                _SCHEMA_PUT_SESSION_INPUT.members["botId"], self.bot_id
            )

        if self.bot_alias_id is not None:
            serializer.write_string(
                _SCHEMA_PUT_SESSION_INPUT.members["botAliasId"], self.bot_alias_id
            )

        if self.locale_id is not None:
            serializer.write_string(
                _SCHEMA_PUT_SESSION_INPUT.members["localeId"], self.locale_id
            )

        if self.session_id is not None:
            serializer.write_string(
                _SCHEMA_PUT_SESSION_INPUT.members["sessionId"], self.session_id
            )

        if self.messages is not None:
            _serialize_messages(
                serializer, _SCHEMA_PUT_SESSION_INPUT.members["messages"], self.messages
            )

        if self.session_state is not None:
            serializer.write_struct(
                _SCHEMA_PUT_SESSION_INPUT.members["sessionState"], self.session_state
            )

        if self.request_attributes is not None:
            _serialize_string_map(
                serializer,
                _SCHEMA_PUT_SESSION_INPUT.members["requestAttributes"],
                self.request_attributes,
            )

        if self.response_content_type is not None:
            serializer.write_string(
                _SCHEMA_PUT_SESSION_INPUT.members["responseContentType"],
                self.response_content_type,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["bot_id"] = de.read_string(
                        _SCHEMA_PUT_SESSION_INPUT.members["botId"]
                    )

                case 1:
                    kwargs["bot_alias_id"] = de.read_string(
                        _SCHEMA_PUT_SESSION_INPUT.members["botAliasId"]
                    )

                case 2:
                    kwargs["locale_id"] = de.read_string(
                        _SCHEMA_PUT_SESSION_INPUT.members["localeId"]
                    )

                case 3:
                    kwargs["session_id"] = de.read_string(
                        _SCHEMA_PUT_SESSION_INPUT.members["sessionId"]
                    )

                case 4:
                    kwargs["messages"] = _deserialize_messages(
                        de, _SCHEMA_PUT_SESSION_INPUT.members["messages"]
                    )

                case 5:
                    kwargs["session_state"] = SessionState.deserialize(de)

                case 6:
                    kwargs["request_attributes"] = _deserialize_string_map(
                        de, _SCHEMA_PUT_SESSION_INPUT.members["requestAttributes"]
                    )

                case 7:
                    kwargs["response_content_type"] = de.read_string(
                        _SCHEMA_PUT_SESSION_INPUT.members["responseContentType"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_PUT_SESSION_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class RecognizeTextInput:
    """Dataclass for RecognizeTextInput structure."""

    bot_id: str | None = None
    """The identifier of the bot that processes the request."""

    bot_alias_id: str | None = None
    """The alias identifier in use for the bot that processes the request."""

    locale_id: str | None = None
    """The locale where the session is in use."""

    session_id: str | None = None
    """The identifier of the user session that is having the conversation."""

    text: str | None = field(repr=False, default=None)
    """The text that the user entered. Amazon Lex V2 interprets this text."""

    session_state: "SessionState | None" = None
    """The current state of the dialog between the user and the bot."""

    request_attributes: dict[str, str] | None = None
    """
    Request-specific information passed between the client application and
    Amazon Lex V2

    The namespace `x-amz-lex:` is reserved for special attributes. Don't
    create any request attributes with the prefix `x-amz-lex:`.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_RECOGNIZE_TEXT_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.bot_id is not None:
            serializer.write_string(
                _SCHEMA_RECOGNIZE_TEXT_INPUT.members["botId"], self.bot_id
            )

        if self.bot_alias_id is not None:
            serializer.write_string(
                _SCHEMA_RECOGNIZE_TEXT_INPUT.members["botAliasId"], self.bot_alias_id
            )

        if self.locale_id is not None:
            serializer.write_string(
                _SCHEMA_RECOGNIZE_TEXT_INPUT.members["localeId"], self.locale_id
            )

        if self.session_id is not None:
            serializer.write_string(
                _SCHEMA_RECOGNIZE_TEXT_INPUT.members["sessionId"], self.session_id
            )

        if self.text is not None:
            serializer.write_string(
                _SCHEMA_RECOGNIZE_TEXT_INPUT.members["text"], self.text
            )

        if self.session_state is not None:
            serializer.write_struct(
                _SCHEMA_RECOGNIZE_TEXT_INPUT.members["sessionState"], self.session_state
            )

        if self.request_attributes is not None:
            _serialize_string_map(
                serializer,
                _SCHEMA_RECOGNIZE_TEXT_INPUT.members["requestAttributes"],
                self.request_attributes,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["bot_id"] = de.read_string(
                        _SCHEMA_RECOGNIZE_TEXT_INPUT.members["botId"]
                    )

                case 1:
                    kwargs["bot_alias_id"] = de.read_string(
                        _SCHEMA_RECOGNIZE_TEXT_INPUT.members["botAliasId"]
                    )

                case 2:
                    kwargs["locale_id"] = de.read_string(
                        _SCHEMA_RECOGNIZE_TEXT_INPUT.members["localeId"]
                    )

                case 3:
                    kwargs["session_id"] = de.read_string(
                        _SCHEMA_RECOGNIZE_TEXT_INPUT.members["sessionId"]
                    )

                case 4:
                    kwargs["text"] = de.read_string(
                        _SCHEMA_RECOGNIZE_TEXT_INPUT.members["text"]
                    )

                case 5:
                    kwargs["session_state"] = SessionState.deserialize(de)

                case 6:
                    kwargs["request_attributes"] = _deserialize_string_map(
                        de, _SCHEMA_RECOGNIZE_TEXT_INPUT.members["requestAttributes"]
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_RECOGNIZE_TEXT_INPUT, consumer=_consumer)
        return kwargs


PUT_SESSION = APIOperation(
    input=PutSessionInput,
    output=PutSessionOutput,
    schema=_SCHEMA_PUT_SESSION,
    input_schema=_SCHEMA_PUT_SESSION_INPUT,
    output_schema=_SCHEMA_PUT_SESSION_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID(
                "com.amazonaws.lexruntimev2#AccessDeniedException"
            ): AccessDeniedException,
            ShapeID(
                "com.amazonaws.lexruntimev2#BadGatewayException"
            ): BadGatewayException,
            ShapeID("com.amazonaws.lexruntimev2#ConflictException"): ConflictException,
            ShapeID(
                "com.amazonaws.lexruntimev2#DependencyFailedException"
            ): DependencyFailedException,
            ShapeID(
                "com.amazonaws.lexruntimev2#InternalServerException"
            ): InternalServerException,
            ShapeID(
                "com.amazonaws.lexruntimev2#ResourceNotFoundException"
            ): ResourceNotFoundException,
            ShapeID(
                "com.amazonaws.lexruntimev2#ThrottlingException"
            ): ThrottlingException,
            ShapeID(
                "com.amazonaws.lexruntimev2#ValidationException"
            ): ValidationException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_ACCESS_DENIED_EXCEPTION,
        _SCHEMA_BAD_GATEWAY_EXCEPTION,
        _SCHEMA_CONFLICT_EXCEPTION,
        _SCHEMA_DEPENDENCY_FAILED_EXCEPTION,
        _SCHEMA_INTERNAL_SERVER_EXCEPTION,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
        _SCHEMA_THROTTLING_EXCEPTION,
        _SCHEMA_VALIDATION_EXCEPTION,
    ],
)


@dataclass
class StartConversationRequestEventStreamConfigurationEvent:
    """
    Configuration information sent from your client application to Amazon
    Lex V2
    """

    value: "ConfigurationEvent"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_START_CONVERSATION_REQUEST_EVENT_STREAM, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_START_CONVERSATION_REQUEST_EVENT_STREAM.members[
                "ConfigurationEvent"
            ],
            self.value,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(value=ConfigurationEvent.deserialize(deserializer))


@dataclass
class StartConversationRequestEventStreamAudioInputEvent:
    """
    Speech audio sent from your client application to Amazon Lex V2. Audio
    starts accumulating when Amazon Lex V2 identifies a voice and continues
    until a natural pause in the speech is found before processing.
    """

    value: AudioInputEvent

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_START_CONVERSATION_REQUEST_EVENT_STREAM, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_START_CONVERSATION_REQUEST_EVENT_STREAM.members["AudioInputEvent"],
            self.value,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(value=AudioInputEvent.deserialize(deserializer))


@dataclass
class StartConversationRequestEventStreamDTMFInputEvent:
    """
    DTMF information sent to Amazon Lex V2 by your application. Amazon Lex
    V2 accumulates the DMTF information from when the user sends the first
    character and ends

    - when there's a pause longer that the value configured for the end
      timeout.

    - when there's a digit that is the configured end character.

    - when Amazon Lex V2 accumulates characters equal to the maximum DTMF
      character configuration.
    """

    value: DTMFInputEvent

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_START_CONVERSATION_REQUEST_EVENT_STREAM, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_START_CONVERSATION_REQUEST_EVENT_STREAM.members["DTMFInputEvent"],
            self.value,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(value=DTMFInputEvent.deserialize(deserializer))


@dataclass
class StartConversationRequestEventStreamTextInputEvent:
    """
    Text sent from your client application to Amazon Lex V2. Each
    `TextInputEvent` is processed individually.
    """

    value: TextInputEvent

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_START_CONVERSATION_REQUEST_EVENT_STREAM, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_START_CONVERSATION_REQUEST_EVENT_STREAM.members["TextInputEvent"],
            self.value,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(value=TextInputEvent.deserialize(deserializer))


@dataclass
class StartConversationRequestEventStreamPlaybackCompletionEvent:
    """
    Event sent from the client application to Amazon Lex V2 to indicate that
    it has finished playing audio and that Amazon Lex V2 should start
    listening for user input.
    """

    value: PlaybackCompletionEvent

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_START_CONVERSATION_REQUEST_EVENT_STREAM, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_START_CONVERSATION_REQUEST_EVENT_STREAM.members[
                "PlaybackCompletionEvent"
            ],
            self.value,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(value=PlaybackCompletionEvent.deserialize(deserializer))


@dataclass
class StartConversationRequestEventStreamDisconnectionEvent:
    """
    Event sent from the client application to indicate to Amazon Lex V2 that
    the conversation is over.
    """

    value: DisconnectionEvent

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_START_CONVERSATION_REQUEST_EVENT_STREAM, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_START_CONVERSATION_REQUEST_EVENT_STREAM.members[
                "DisconnectionEvent"
            ],
            self.value,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(value=DisconnectionEvent.deserialize(deserializer))


@dataclass
class StartConversationRequestEventStreamUnknown:
    """
    Represents an unknown variant.

    If you receive this value, you will need to update your library to receive the
    parsed value.

    This value may not be deliberately sent.
    """

    tag: str

    def serialize(self, serializer: ShapeSerializer):
        raise SerializationError("Unknown union variants may not be serialized.")

    def serialize_members(self, serializer: ShapeSerializer):
        raise SerializationError("Unknown union variants may not be serialized.")

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        raise NotImplementedError()


StartConversationRequestEventStream = Union[
    StartConversationRequestEventStreamConfigurationEvent
    | StartConversationRequestEventStreamAudioInputEvent
    | StartConversationRequestEventStreamDTMFInputEvent
    | StartConversationRequestEventStreamTextInputEvent
    | StartConversationRequestEventStreamPlaybackCompletionEvent
    | StartConversationRequestEventStreamDisconnectionEvent
    | StartConversationRequestEventStreamUnknown
]
"""
Represents a stream of events between your application and Amazon Lex
V2.
"""


class _StartConversationRequestEventStreamDeserializer:
    _result: StartConversationRequestEventStream | None = None

    def deserialize(
        self, deserializer: ShapeDeserializer
    ) -> StartConversationRequestEventStream:
        self._result = None
        deserializer.read_struct(
            _SCHEMA_START_CONVERSATION_REQUEST_EVENT_STREAM, self._consumer
        )

        if self._result is None:
            raise SerializationError(
                "Unions must have exactly one value, but found none."
            )

        return self._result

    def _consumer(self, schema: Schema, de: ShapeDeserializer) -> None:
        match schema.expect_member_index():
            case 0:
                self._set_result(
                    StartConversationRequestEventStreamConfigurationEvent.deserialize(
                        de
                    )
                )

            case 1:
                self._set_result(
                    StartConversationRequestEventStreamAudioInputEvent.deserialize(de)
                )

            case 2:
                self._set_result(
                    StartConversationRequestEventStreamDTMFInputEvent.deserialize(de)
                )

            case 3:
                self._set_result(
                    StartConversationRequestEventStreamTextInputEvent.deserialize(de)
                )

            case 4:
                self._set_result(
                    StartConversationRequestEventStreamPlaybackCompletionEvent.deserialize(
                        de
                    )
                )

            case 5:
                self._set_result(
                    StartConversationRequestEventStreamDisconnectionEvent.deserialize(
                        de
                    )
                )

            case _:
                self._set_result(
                    StartConversationRequestEventStreamUnknown(
                        tag=schema.expect_member_name()
                    )
                )

    def _set_result(self, value: StartConversationRequestEventStream) -> None:
        if self._result is not None:
            raise SerializationError(
                "Unions must have exactly one value, but found more than one."
            )
        self._result = value


@dataclass(kw_only=True)
class StartConversationInput:
    """Dataclass for StartConversationInput structure."""

    bot_id: str | None = None
    """The identifier of the bot to process the request."""

    bot_alias_id: str | None = None
    """The alias identifier in use for the bot that processes the request."""

    locale_id: str | None = None
    """The locale where the session is in use."""

    session_id: str | None = None
    """The identifier of the user session that is having the conversation."""

    conversation_mode: str | None = None
    """
    The conversation type that you are using the Amazon Lex V2. If the
    conversation mode is `AUDIO` you can send both audio and DTMF
    information. If the mode is `TEXT` you can only send text.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_START_CONVERSATION_INPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.bot_id is not None:
            serializer.write_string(
                _SCHEMA_START_CONVERSATION_INPUT.members["botId"], self.bot_id
            )

        if self.bot_alias_id is not None:
            serializer.write_string(
                _SCHEMA_START_CONVERSATION_INPUT.members["botAliasId"],
                self.bot_alias_id,
            )

        if self.locale_id is not None:
            serializer.write_string(
                _SCHEMA_START_CONVERSATION_INPUT.members["localeId"], self.locale_id
            )

        if self.session_id is not None:
            serializer.write_string(
                _SCHEMA_START_CONVERSATION_INPUT.members["sessionId"], self.session_id
            )

        if self.conversation_mode is not None:
            serializer.write_string(
                _SCHEMA_START_CONVERSATION_INPUT.members["conversationMode"],
                self.conversation_mode,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["bot_id"] = de.read_string(
                        _SCHEMA_START_CONVERSATION_INPUT.members["botId"]
                    )

                case 1:
                    kwargs["bot_alias_id"] = de.read_string(
                        _SCHEMA_START_CONVERSATION_INPUT.members["botAliasId"]
                    )

                case 2:
                    kwargs["locale_id"] = de.read_string(
                        _SCHEMA_START_CONVERSATION_INPUT.members["localeId"]
                    )

                case 3:
                    kwargs["session_id"] = de.read_string(
                        _SCHEMA_START_CONVERSATION_INPUT.members["sessionId"]
                    )

                case 4:
                    kwargs["conversation_mode"] = ConversationMode(
                        de.read_string(
                            _SCHEMA_START_CONVERSATION_INPUT.members["conversationMode"]
                        )
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_START_CONVERSATION_INPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class GetSessionOutput:
    """Dataclass for GetSessionOutput structure."""

    session_id: str | None = None
    """The identifier of the returned session."""

    messages: list[Message] | None = None
    """
    A list of messages that were last sent to the user. The messages are
    ordered based on the order that your returned the messages from your
    Lambda function or the order that messages are defined in the bot.
    """

    interpretations: "list[Interpretation] | None" = None
    """
    A list of intents that Amazon Lex V2 determined might satisfy the
    user's utterance.

    Each interpretation includes the intent, a score that indicates how
    confident Amazon Lex V2 is that the interpretation is the correct one,
    and an optional sentiment response that indicates the sentiment
    expressed in the utterance.
    """

    session_state: "SessionState | None" = None
    """
    Represents the current state of the dialog between the user and the bot.

    You can use this to determine the progress of the conversation and what
    the next action might be.
    """

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_GET_SESSION_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.session_id is not None:
            serializer.write_string(
                _SCHEMA_GET_SESSION_OUTPUT.members["sessionId"], self.session_id
            )

        if self.messages is not None:
            _serialize_messages(
                serializer,
                _SCHEMA_GET_SESSION_OUTPUT.members["messages"],
                self.messages,
            )

        if self.interpretations is not None:
            _serialize_interpretations(
                serializer,
                _SCHEMA_GET_SESSION_OUTPUT.members["interpretations"],
                self.interpretations,
            )

        if self.session_state is not None:
            serializer.write_struct(
                _SCHEMA_GET_SESSION_OUTPUT.members["sessionState"], self.session_state
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["session_id"] = de.read_string(
                        _SCHEMA_GET_SESSION_OUTPUT.members["sessionId"]
                    )

                case 1:
                    kwargs["messages"] = _deserialize_messages(
                        de, _SCHEMA_GET_SESSION_OUTPUT.members["messages"]
                    )

                case 2:
                    kwargs["interpretations"] = _deserialize_interpretations(
                        de, _SCHEMA_GET_SESSION_OUTPUT.members["interpretations"]
                    )

                case 3:
                    kwargs["session_state"] = SessionState.deserialize(de)

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_GET_SESSION_OUTPUT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class IntentResultEvent:
    """
    Contains the current state of the conversation between the client
    application and Amazon Lex V2.
    """

    input_mode: str | None = None
    """
    Indicates whether the input to the operation was text, speech, or from a
    touch-tone keypad.
    """

    interpretations: "list[Interpretation] | None" = None
    """
    A list of intents that Amazon Lex V2 determined might satisfy the
    user's utterance.

    Each interpretation includes the intent, a score that indicates how
    confident Amazon Lex V2 is that the interpretation is the correct one,
    and an optional sentiment response that indicates the sentiment
    expressed in the utterance.
    """

    session_state: "SessionState | None" = None
    """The state of the user's session with Amazon Lex V2."""

    request_attributes: dict[str, str] | None = None
    """The attributes sent in the request."""

    session_id: str | None = None
    """The identifier of the session in use."""

    event_id: str | None = None
    """
    A unique identifier of the event sent by Amazon Lex V2. The identifier
    is in the form `RESPONSE-N`, where N is a number starting with one and
    incremented for each event sent by Amazon Lex V2 in the current session.
    """

    recognized_bot_member: RecognizedBotMember | None = None
    """The bot member that is processing the intent."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_INTENT_RESULT_EVENT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.input_mode is not None:
            serializer.write_string(
                _SCHEMA_INTENT_RESULT_EVENT.members["inputMode"], self.input_mode
            )

        if self.interpretations is not None:
            _serialize_interpretations(
                serializer,
                _SCHEMA_INTENT_RESULT_EVENT.members["interpretations"],
                self.interpretations,
            )

        if self.session_state is not None:
            serializer.write_struct(
                _SCHEMA_INTENT_RESULT_EVENT.members["sessionState"], self.session_state
            )

        if self.request_attributes is not None:
            _serialize_string_map(
                serializer,
                _SCHEMA_INTENT_RESULT_EVENT.members["requestAttributes"],
                self.request_attributes,
            )

        if self.session_id is not None:
            serializer.write_string(
                _SCHEMA_INTENT_RESULT_EVENT.members["sessionId"], self.session_id
            )

        if self.event_id is not None:
            serializer.write_string(
                _SCHEMA_INTENT_RESULT_EVENT.members["eventId"], self.event_id
            )

        if self.recognized_bot_member is not None:
            serializer.write_struct(
                _SCHEMA_INTENT_RESULT_EVENT.members["recognizedBotMember"],
                self.recognized_bot_member,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["input_mode"] = InputMode(
                        de.read_string(_SCHEMA_INTENT_RESULT_EVENT.members["inputMode"])
                    )

                case 1:
                    kwargs["interpretations"] = _deserialize_interpretations(
                        de, _SCHEMA_INTENT_RESULT_EVENT.members["interpretations"]
                    )

                case 2:
                    kwargs["session_state"] = SessionState.deserialize(de)

                case 3:
                    kwargs["request_attributes"] = _deserialize_string_map(
                        de, _SCHEMA_INTENT_RESULT_EVENT.members["requestAttributes"]
                    )

                case 4:
                    kwargs["session_id"] = de.read_string(
                        _SCHEMA_INTENT_RESULT_EVENT.members["sessionId"]
                    )

                case 5:
                    kwargs["event_id"] = de.read_string(
                        _SCHEMA_INTENT_RESULT_EVENT.members["eventId"]
                    )

                case 6:
                    kwargs["recognized_bot_member"] = RecognizedBotMember.deserialize(
                        de
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_INTENT_RESULT_EVENT, consumer=_consumer)
        return kwargs


@dataclass(kw_only=True)
class RecognizeTextOutput:
    """Dataclass for RecognizeTextOutput structure."""

    messages: list[Message] | None = None
    """
    A list of messages last sent to the user. The messages are ordered based
    on the order that you returned the messages from your Lambda function or
    the order that the messages are defined in the bot.
    """

    session_state: "SessionState | None" = None
    """
    Represents the current state of the dialog between the user and the bot.

    Use this to determine the progress of the conversation and what the next
    action may be.
    """

    interpretations: "list[Interpretation] | None" = None
    """
    A list of intents that Amazon Lex V2 determined might satisfy the
    user's utterance.

    Each interpretation includes the intent, a score that indicates now
    confident Amazon Lex V2 is that the interpretation is the correct one,
    and an optional sentiment response that indicates the sentiment
    expressed in the utterance.
    """

    request_attributes: dict[str, str] | None = None
    """The attributes sent in the request."""

    session_id: str | None = None
    """The identifier of the session in use."""

    recognized_bot_member: RecognizedBotMember | None = None
    """The bot member that recognized the text."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_RECOGNIZE_TEXT_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        if self.messages is not None:
            _serialize_messages(
                serializer,
                _SCHEMA_RECOGNIZE_TEXT_OUTPUT.members["messages"],
                self.messages,
            )

        if self.session_state is not None:
            serializer.write_struct(
                _SCHEMA_RECOGNIZE_TEXT_OUTPUT.members["sessionState"],
                self.session_state,
            )

        if self.interpretations is not None:
            _serialize_interpretations(
                serializer,
                _SCHEMA_RECOGNIZE_TEXT_OUTPUT.members["interpretations"],
                self.interpretations,
            )

        if self.request_attributes is not None:
            _serialize_string_map(
                serializer,
                _SCHEMA_RECOGNIZE_TEXT_OUTPUT.members["requestAttributes"],
                self.request_attributes,
            )

        if self.session_id is not None:
            serializer.write_string(
                _SCHEMA_RECOGNIZE_TEXT_OUTPUT.members["sessionId"], self.session_id
            )

        if self.recognized_bot_member is not None:
            serializer.write_struct(
                _SCHEMA_RECOGNIZE_TEXT_OUTPUT.members["recognizedBotMember"],
                self.recognized_bot_member,
            )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case 0:
                    kwargs["messages"] = _deserialize_messages(
                        de, _SCHEMA_RECOGNIZE_TEXT_OUTPUT.members["messages"]
                    )

                case 1:
                    kwargs["session_state"] = SessionState.deserialize(de)

                case 2:
                    kwargs["interpretations"] = _deserialize_interpretations(
                        de, _SCHEMA_RECOGNIZE_TEXT_OUTPUT.members["interpretations"]
                    )

                case 3:
                    kwargs["request_attributes"] = _deserialize_string_map(
                        de, _SCHEMA_RECOGNIZE_TEXT_OUTPUT.members["requestAttributes"]
                    )

                case 4:
                    kwargs["session_id"] = de.read_string(
                        _SCHEMA_RECOGNIZE_TEXT_OUTPUT.members["sessionId"]
                    )

                case 5:
                    kwargs["recognized_bot_member"] = RecognizedBotMember.deserialize(
                        de
                    )

                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_RECOGNIZE_TEXT_OUTPUT, consumer=_consumer)
        return kwargs


GET_SESSION = APIOperation(
    input=GetSessionInput,
    output=GetSessionOutput,
    schema=_SCHEMA_GET_SESSION,
    input_schema=_SCHEMA_GET_SESSION_INPUT,
    output_schema=_SCHEMA_GET_SESSION_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID(
                "com.amazonaws.lexruntimev2#AccessDeniedException"
            ): AccessDeniedException,
            ShapeID(
                "com.amazonaws.lexruntimev2#InternalServerException"
            ): InternalServerException,
            ShapeID(
                "com.amazonaws.lexruntimev2#ResourceNotFoundException"
            ): ResourceNotFoundException,
            ShapeID(
                "com.amazonaws.lexruntimev2#ThrottlingException"
            ): ThrottlingException,
            ShapeID(
                "com.amazonaws.lexruntimev2#ValidationException"
            ): ValidationException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_ACCESS_DENIED_EXCEPTION,
        _SCHEMA_INTERNAL_SERVER_EXCEPTION,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
        _SCHEMA_THROTTLING_EXCEPTION,
        _SCHEMA_VALIDATION_EXCEPTION,
    ],
)


@dataclass
class StartConversationResponseEventStreamPlaybackInterruptionEvent:
    """
    Event sent from Amazon Lex V2 to indicate to the client application
    should stop playback of audio. For example, if the client is playing a
    prompt that asks for the user's telephone number, the user might start
    to say the phone number before the prompt is complete. Amazon Lex V2
    sends this event to the client application to indicate that the user is
    responding and that Amazon Lex V2 is processing their input.
    """

    value: PlaybackInterruptionEvent

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_START_CONVERSATION_RESPONSE_EVENT_STREAM, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_START_CONVERSATION_RESPONSE_EVENT_STREAM.members[
                "PlaybackInterruptionEvent"
            ],
            self.value,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(value=PlaybackInterruptionEvent.deserialize(deserializer))


@dataclass
class StartConversationResponseEventStreamTranscriptEvent:
    """
    Event sent from Amazon Lex V2 to your client application that contains a
    transcript of voice audio.
    """

    value: TranscriptEvent

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_START_CONVERSATION_RESPONSE_EVENT_STREAM, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_START_CONVERSATION_RESPONSE_EVENT_STREAM.members["TranscriptEvent"],
            self.value,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(value=TranscriptEvent.deserialize(deserializer))


@dataclass
class StartConversationResponseEventStreamIntentResultEvent:
    """
    Event sent from Amazon Lex V2 to the client application containing the
    current state of the conversation between the user and Amazon Lex V2.
    """

    value: "IntentResultEvent"

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_START_CONVERSATION_RESPONSE_EVENT_STREAM, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_START_CONVERSATION_RESPONSE_EVENT_STREAM.members[
                "IntentResultEvent"
            ],
            self.value,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(value=IntentResultEvent.deserialize(deserializer))


@dataclass
class StartConversationResponseEventStreamTextResponseEvent:
    """
    The event sent from Amazon Lex V2 to your application with text to
    present to the user.
    """

    value: TextResponseEvent

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_START_CONVERSATION_RESPONSE_EVENT_STREAM, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_START_CONVERSATION_RESPONSE_EVENT_STREAM.members[
                "TextResponseEvent"
            ],
            self.value,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(value=TextResponseEvent.deserialize(deserializer))


@dataclass
class StartConversationResponseEventStreamAudioResponseEvent:
    """
    An event sent from Amazon Lex V2 to your client application containing
    audio to play to the user.
    """

    value: AudioResponseEvent

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_START_CONVERSATION_RESPONSE_EVENT_STREAM, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_START_CONVERSATION_RESPONSE_EVENT_STREAM.members[
                "AudioResponseEvent"
            ],
            self.value,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(value=AudioResponseEvent.deserialize(deserializer))


@dataclass
class StartConversationResponseEventStreamHeartbeatEvent:
    """
    Event that Amazon Lex V2 sends to indicate that the stream is still open
    between the client application and Amazon Lex V2
    """

    value: HeartbeatEvent

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_START_CONVERSATION_RESPONSE_EVENT_STREAM, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_START_CONVERSATION_RESPONSE_EVENT_STREAM.members["HeartbeatEvent"],
            self.value,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(value=HeartbeatEvent.deserialize(deserializer))


@dataclass
class StartConversationResponseEventStreamAccessDeniedException:
    """
    Exception thrown when the credentials passed with the request are
    invalid or expired. Also thrown when the credentials in the request do
    not have permission to access the `StartConversation` operation.
    """

    value: AccessDeniedException

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_START_CONVERSATION_RESPONSE_EVENT_STREAM, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_START_CONVERSATION_RESPONSE_EVENT_STREAM.members[
                "AccessDeniedException"
            ],
            self.value,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(value=AccessDeniedException.deserialize(deserializer))


@dataclass
class StartConversationResponseEventStreamResourceNotFoundException:
    """
    Exception thrown if one of the input parameters points to a resource
    that does not exist. For example, if the bot ID specified does not
    exist.
    """

    value: ResourceNotFoundException

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_START_CONVERSATION_RESPONSE_EVENT_STREAM, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_START_CONVERSATION_RESPONSE_EVENT_STREAM.members[
                "ResourceNotFoundException"
            ],
            self.value,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(value=ResourceNotFoundException.deserialize(deserializer))


@dataclass
class StartConversationResponseEventStreamValidationException:
    """
    Exception thrown when one or more parameters could not be validated. The
    `message` contains the name of the field that isn't valid.
    """

    value: ValidationException

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_START_CONVERSATION_RESPONSE_EVENT_STREAM, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_START_CONVERSATION_RESPONSE_EVENT_STREAM.members[
                "ValidationException"
            ],
            self.value,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(value=ValidationException.deserialize(deserializer))


@dataclass
class StartConversationResponseEventStreamThrottlingException:
    """
    Exception thrown when your application exceeds the maximum number of
    concurrent requests.
    """

    value: ThrottlingException

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_START_CONVERSATION_RESPONSE_EVENT_STREAM, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_START_CONVERSATION_RESPONSE_EVENT_STREAM.members[
                "ThrottlingException"
            ],
            self.value,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(value=ThrottlingException.deserialize(deserializer))


@dataclass
class StartConversationResponseEventStreamInternalServerException:
    """An error occurred with Amazon Lex V2."""

    value: InternalServerException

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_START_CONVERSATION_RESPONSE_EVENT_STREAM, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_START_CONVERSATION_RESPONSE_EVENT_STREAM.members[
                "InternalServerException"
            ],
            self.value,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(value=InternalServerException.deserialize(deserializer))


@dataclass
class StartConversationResponseEventStreamConflictException:
    """
    Exception thrown when two clients are using the same AWS account, Amazon
    Lex V2 bot, and session ID.
    """

    value: ConflictException

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_START_CONVERSATION_RESPONSE_EVENT_STREAM, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_START_CONVERSATION_RESPONSE_EVENT_STREAM.members[
                "ConflictException"
            ],
            self.value,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(value=ConflictException.deserialize(deserializer))


@dataclass
class StartConversationResponseEventStreamDependencyFailedException:
    """"""

    value: DependencyFailedException

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_START_CONVERSATION_RESPONSE_EVENT_STREAM, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_START_CONVERSATION_RESPONSE_EVENT_STREAM.members[
                "DependencyFailedException"
            ],
            self.value,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(value=DependencyFailedException.deserialize(deserializer))


@dataclass
class StartConversationResponseEventStreamBadGatewayException:
    """"""

    value: BadGatewayException

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_START_CONVERSATION_RESPONSE_EVENT_STREAM, self)

    def serialize_members(self, serializer: ShapeSerializer):
        serializer.write_struct(
            _SCHEMA_START_CONVERSATION_RESPONSE_EVENT_STREAM.members[
                "BadGatewayException"
            ],
            self.value,
        )

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(value=BadGatewayException.deserialize(deserializer))


@dataclass
class StartConversationResponseEventStreamUnknown:
    """
    Represents an unknown variant.

    If you receive this value, you will need to update your library to receive the
    parsed value.

    This value may not be deliberately sent.
    """

    tag: str

    def serialize(self, serializer: ShapeSerializer):
        raise SerializationError("Unknown union variants may not be serialized.")

    def serialize_members(self, serializer: ShapeSerializer):
        raise SerializationError("Unknown union variants may not be serialized.")

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        raise NotImplementedError()


StartConversationResponseEventStream = Union[
    StartConversationResponseEventStreamPlaybackInterruptionEvent
    | StartConversationResponseEventStreamTranscriptEvent
    | StartConversationResponseEventStreamIntentResultEvent
    | StartConversationResponseEventStreamTextResponseEvent
    | StartConversationResponseEventStreamAudioResponseEvent
    | StartConversationResponseEventStreamHeartbeatEvent
    | StartConversationResponseEventStreamAccessDeniedException
    | StartConversationResponseEventStreamResourceNotFoundException
    | StartConversationResponseEventStreamValidationException
    | StartConversationResponseEventStreamThrottlingException
    | StartConversationResponseEventStreamInternalServerException
    | StartConversationResponseEventStreamConflictException
    | StartConversationResponseEventStreamDependencyFailedException
    | StartConversationResponseEventStreamBadGatewayException
    | StartConversationResponseEventStreamUnknown
]
"""
Represents a stream of events between Amazon Lex V2 and your
application.
"""


class _StartConversationResponseEventStreamDeserializer:
    _result: StartConversationResponseEventStream | None = None

    def deserialize(
        self, deserializer: ShapeDeserializer
    ) -> StartConversationResponseEventStream:
        self._result = None
        deserializer.read_struct(
            _SCHEMA_START_CONVERSATION_RESPONSE_EVENT_STREAM, self._consumer
        )

        if self._result is None:
            raise SerializationError(
                "Unions must have exactly one value, but found none."
            )

        return self._result

    def _consumer(self, schema: Schema, de: ShapeDeserializer) -> None:
        match schema.expect_member_index():
            case 0:
                self._set_result(
                    StartConversationResponseEventStreamPlaybackInterruptionEvent.deserialize(
                        de
                    )
                )

            case 1:
                self._set_result(
                    StartConversationResponseEventStreamTranscriptEvent.deserialize(de)
                )

            case 2:
                self._set_result(
                    StartConversationResponseEventStreamIntentResultEvent.deserialize(
                        de
                    )
                )

            case 3:
                self._set_result(
                    StartConversationResponseEventStreamTextResponseEvent.deserialize(
                        de
                    )
                )

            case 4:
                self._set_result(
                    StartConversationResponseEventStreamAudioResponseEvent.deserialize(
                        de
                    )
                )

            case 5:
                self._set_result(
                    StartConversationResponseEventStreamHeartbeatEvent.deserialize(de)
                )

            case 6:
                self._set_result(
                    StartConversationResponseEventStreamAccessDeniedException.deserialize(
                        de
                    )
                )

            case 7:
                self._set_result(
                    StartConversationResponseEventStreamResourceNotFoundException.deserialize(
                        de
                    )
                )

            case 8:
                self._set_result(
                    StartConversationResponseEventStreamValidationException.deserialize(
                        de
                    )
                )

            case 9:
                self._set_result(
                    StartConversationResponseEventStreamThrottlingException.deserialize(
                        de
                    )
                )

            case 10:
                self._set_result(
                    StartConversationResponseEventStreamInternalServerException.deserialize(
                        de
                    )
                )

            case 11:
                self._set_result(
                    StartConversationResponseEventStreamConflictException.deserialize(
                        de
                    )
                )

            case 12:
                self._set_result(
                    StartConversationResponseEventStreamDependencyFailedException.deserialize(
                        de
                    )
                )

            case 13:
                self._set_result(
                    StartConversationResponseEventStreamBadGatewayException.deserialize(
                        de
                    )
                )

            case _:
                self._set_result(
                    StartConversationResponseEventStreamUnknown(
                        tag=schema.expect_member_name()
                    )
                )

    def _set_result(self, value: StartConversationResponseEventStream) -> None:
        if self._result is not None:
            raise SerializationError(
                "Unions must have exactly one value, but found more than one."
            )
        self._result = value


@dataclass(kw_only=True)
class StartConversationOutput:
    """Dataclass for StartConversationOutput structure."""

    def serialize(self, serializer: ShapeSerializer):
        serializer.write_struct(_SCHEMA_START_CONVERSATION_OUTPUT, self)

    def serialize_members(self, serializer: ShapeSerializer):
        pass

    @classmethod
    def deserialize(cls, deserializer: ShapeDeserializer) -> Self:
        return cls(**cls.deserialize_kwargs(deserializer))

    @classmethod
    def deserialize_kwargs(cls, deserializer: ShapeDeserializer) -> dict[str, Any]:
        kwargs: dict[str, Any] = {}

        def _consumer(schema: Schema, de: ShapeDeserializer) -> None:
            match schema.expect_member_index():
                case _:
                    logger.debug("Unexpected member schema: %s", schema)

        deserializer.read_struct(_SCHEMA_START_CONVERSATION_OUTPUT, consumer=_consumer)
        return kwargs


RECOGNIZE_TEXT = APIOperation(
    input=RecognizeTextInput,
    output=RecognizeTextOutput,
    schema=_SCHEMA_RECOGNIZE_TEXT,
    input_schema=_SCHEMA_RECOGNIZE_TEXT_INPUT,
    output_schema=_SCHEMA_RECOGNIZE_TEXT_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID(
                "com.amazonaws.lexruntimev2#AccessDeniedException"
            ): AccessDeniedException,
            ShapeID(
                "com.amazonaws.lexruntimev2#BadGatewayException"
            ): BadGatewayException,
            ShapeID("com.amazonaws.lexruntimev2#ConflictException"): ConflictException,
            ShapeID(
                "com.amazonaws.lexruntimev2#DependencyFailedException"
            ): DependencyFailedException,
            ShapeID(
                "com.amazonaws.lexruntimev2#InternalServerException"
            ): InternalServerException,
            ShapeID(
                "com.amazonaws.lexruntimev2#ResourceNotFoundException"
            ): ResourceNotFoundException,
            ShapeID(
                "com.amazonaws.lexruntimev2#ThrottlingException"
            ): ThrottlingException,
            ShapeID(
                "com.amazonaws.lexruntimev2#ValidationException"
            ): ValidationException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_ACCESS_DENIED_EXCEPTION,
        _SCHEMA_BAD_GATEWAY_EXCEPTION,
        _SCHEMA_CONFLICT_EXCEPTION,
        _SCHEMA_DEPENDENCY_FAILED_EXCEPTION,
        _SCHEMA_INTERNAL_SERVER_EXCEPTION,
        _SCHEMA_RESOURCE_NOT_FOUND_EXCEPTION,
        _SCHEMA_THROTTLING_EXCEPTION,
        _SCHEMA_VALIDATION_EXCEPTION,
    ],
)

START_CONVERSATION = APIOperation(
    input=StartConversationInput,
    output=StartConversationOutput,
    schema=_SCHEMA_START_CONVERSATION,
    input_schema=_SCHEMA_START_CONVERSATION_INPUT,
    output_schema=_SCHEMA_START_CONVERSATION_OUTPUT,
    error_registry=TypeRegistry(
        {
            ShapeID(
                "com.amazonaws.lexruntimev2#AccessDeniedException"
            ): AccessDeniedException,
            ShapeID(
                "com.amazonaws.lexruntimev2#InternalServerException"
            ): InternalServerException,
            ShapeID(
                "com.amazonaws.lexruntimev2#ThrottlingException"
            ): ThrottlingException,
            ShapeID(
                "com.amazonaws.lexruntimev2#ValidationException"
            ): ValidationException,
        }
    ),
    effective_auth_schemes=[ShapeID("aws.auth#sigv4")],
    error_schemas=[
        _SCHEMA_ACCESS_DENIED_EXCEPTION,
        _SCHEMA_INTERNAL_SERVER_EXCEPTION,
        _SCHEMA_THROTTLING_EXCEPTION,
        _SCHEMA_VALIDATION_EXCEPTION,
    ],
)
